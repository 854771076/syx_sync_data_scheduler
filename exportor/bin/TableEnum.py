
from enum import Enum
class TableEnum(Enum):
    dwd_essential_info_firm_branch_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255)  NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT NULL COMMENT "加密唯一id",
    `firm_eid` varchar(64)  NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_no` varchar(64) NULL COMMENT "组织机构代码",
    `br_name` varchar(255) NULL COMMENT "分支机构名称",
    `br_id` varchar(64)  NULL COMMENT "分支机构eid",
    `br_usc_code` varchar(64) NULL COMMENT "分支机构统一社会信用代码",
    `br_reg_no` varchar(64) NULL COMMENT "分支机构注册号",
    `br_org_no` varchar(64) NULL COMMENT "分支机构注册号",
    `belong_org` varchar(64) NULL COMMENT "所属机构",
    `oper_name` varchar(255) NULL COMMENT "法定代表人",
    `is_history` int NULL COMMENT "是否历史记录",
    `source` varchar(64) NULL COMMENT "数据来源",
	`local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `ope_status` int NULL COMMENT "分支机构经营状态",
    `est_date` date NULL COMMENT "分支机构成立时间",
    `province` varchar(64) NULL COMMENT "分支机构所在省份",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC  comment = "基本信息-分支机构"'''

    dwd_essential_info_commercial_reg_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64)  NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NOT NULL COMMENT "企业eid",
    `firm_id` varchar(64)  NULL COMMENT "企业id",
    `firm_name` varchar(1024)  NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(255) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `tax_no` varchar(64) NULL COMMENT "纳税人识别号",
    `firm_name_sh` varchar(64) NULL COMMENT "企业名称简称",
    `firm_name_en` varchar(255) NULL COMMENT "企业英文名称",
    `firm_category` varchar(64) NULL COMMENT "企业/个体户 ",
    `ope_status` int NULL COMMENT "经营状态",
    `legal_rep` varchar(512) NULL COMMENT "法定代表人",
    `legal_rep_id` varchar(64) NULL COMMENT "法定代表人id",
    `legal_rep_type` varchar(64) NULL COMMENT "法定代表人类型",
    `legal_rep_category` varchar(64) NULL COMMENT "法定代表人主体类型",
    `firm_type` varchar(64) NULL COMMENT "企业类型",
    `firm_type_code` varchar(64) NULL COMMENT "企业类型编码",
    `firm_org_type` varchar(64) NULL COMMENT "企业组织类型",
    `firm_properties` varchar(64) NULL COMMENT "企业性质",
    `est_date` date NULL COMMENT "成立日期",
    `cancel_date` date NULL COMMENT "注销日期",
    `cancel_reason` varchar(512) NULL COMMENT "注销原因",
    `rev_date` date NULL COMMENT "吊销日期",
    `rev_certificates` varchar(255) NULL COMMENT "吊销凭证",
    `reg_agency` varchar(255) NULL COMMENT "登记机关",
    `reg_agency_area_code` varchar(64) NULL COMMENT "登记机关区域码",
    `usc_code_area_code` varchar(64) NULL COMMENT "社会信用代码区域码",
    `ind_econ_code` varchar(64) NULL COMMENT "所属行业代码",
    `ind_econ_name` varchar(64) NULL COMMENT "所属行业名称",
    `appr_date` date NULL COMMENT "核准日期",
    `emp_num` varchar(64) NULL COMMENT "员工人数",
    `insured_emp_num` int NULL COMMENT "参保人数",
    `ope_address` varchar(512) NULL COMMENT "注册地址",
    `reg_address` varchar(1000) NULL COMMENT "经营地址",
    `ope_addr_code` varchar(64) NULL COMMENT "注册地址行政区划",
    `reg_addr_code` varchar(64) NULL COMMENT "经营地址行政区划",
    `open_date` date NULL COMMENT "营业开始日期",
    `close_date` date NULL COMMENT "营业结束日期",
    `ope_period` varchar(64) NULL COMMENT "营业期限",
    `province` varchar(64) NULL COMMENT "所在省份",
    `city` varchar(64) NULL COMMENT "所在城市",
    `district` varchar(64) NULL COMMENT "所在区县",
    `bd_longitude` varchar(64) NULL COMMENT "百度经度",
    `bd_latitude` varchar(64) NULL COMMENT "百度纬度",
    `gd_longitude` varchar(64) NULL COMMENT "高德经度",
    `gd_latitude` varchar(64) NULL COMMENT "高德纬度",
    `reg_cap` decimal(38, 6) NULL COMMENT "注册资本",
    `reg_cap_cur` varchar(64) NULL COMMENT "注册资本币种",
    `rec_cap` varchar(64) NULL COMMENT "实收资本",
    `ope_scope` text NULL COMMENT "经营范围",
    `firm_scale` varchar(64) NULL COMMENT "企业规模",
    `parent_name` varchar(64) NULL COMMENT "上级机构名称",
    `parent_type` varchar(64) NULL COMMENT "上级机构类型",
    `profile` varchar(64) NULL COMMENT "公司简介",
    `firm_name_used` varchar(1000) NULL COMMENT "曾用名",
    `logo_url` varchar(255) NULL COMMENT "logo图片地址",
    `is_history` int  NULL COMMENT "是否历史记录",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `telephone` varchar(64) NULL COMMENT "企业联系电话",
    `unq_id` varchar(64)  NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64)  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `email` varchar(255) NULL COMMENT "电子邮箱",
    `gb_province` varchar(64) NULL COMMENT "nan",
    `gb_city` varchar(64) NULL COMMENT "nan",
    `gb_district` varchar(64) NULL COMMENT "nan",
    `md5_reg_address` varchar(64) NULL COMMENT "nan",
    PRIMARY KEY (firm_eid)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC  comment = "基本信息-工商基本信息"'''

    dwd_essential_info_shareholder_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NOT  NULL COMMENT "唯一id",
    `firm_eid` varchar(64)  NULL COMMENT "企业eid",
    `firm_id` varchar(64)  NULL COMMENT "企业id",
    `firm_name` varchar(64)  NULL COMMENT "企业名称",
    `usc_code` varchar(64)  NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64)  NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `sha_name` varchar(255) NULL COMMENT "股东名称",
    `sha_type` varchar(64) NULL COMMENT "股东类型",
    `sha_id_type` varchar(64) NULL COMMENT "股东证件类型",
    `sha_id_no` varchar(64) NULL COMMENT "股东证件号码",
    `sha_id` varchar(64) NULL COMMENT "股东id",
    `hold_ratio` decimal(18, 6) NULL COMMENT "持股比例",
    `should_cap_items` text NULL COMMENT "认缴出资明细",
    `cumulative_amt` decimal(18, 6) NULL COMMENT "累计认缴额",
    `cumulative_amt_cur` varchar(64) NULL COMMENT "累计认缴币种",
    `real_cap_items` text NULL COMMENT "实缴出资明细",
    `acc_real_cap` decimal(18, 6) NULL COMMENT "累计实缴额",
    `acc_real_cap_cur` varchar(64) NULL COMMENT "累计实缴币种",
    `sha_start_date` date NULL COMMENT "开始参股时间",
    `sha_end_date` date NULL COMMENT "股东退出时间",
    `country` varchar(64) NULL COMMENT "国别",
    `is_history` int NULL COMMENT "是否历史",
    `hold_ratio_source` int NULL COMMENT "股比数据来源",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据更新时间",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC  comment = "基本信息-股东信息"'''

    dwd_essential_info_business_alter = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(255) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `alt_date` varchar(64) NULL COMMENT "变更日期",
    `alt_item` varchar(255) NULL COMMENT "变更事项",
    `alt_type` varchar(64) NULL COMMENT "变更类型",
    `alt_before` longtext NULL COMMENT "变更前内容",
    `alt_after` longtext NULL COMMENT "变更后内容",
    `is_history` int NULL COMMENT "是否历史记录",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "基本信息-企业变更记录"'''

    dwd_essential_info_firm_year_report = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT NULL COMMENT "加密唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `report_year` varchar(64) NULL COMMENT "年报年份",
    `report_name` varchar(64) NULL COMMENT "年报名称",
    `report_date` varchar(64) NULL COMMENT "年报日期",
    `net_amount` varchar(64) NULL COMMENT "净利润",
    `debit_amount` varchar(64) NULL COMMENT "负债总额",
    `prac_person_num` varchar(64) NULL COMMENT "实际员工数量",
    `telephone` varchar(255) NULL COMMENT "公司电话",
    `email` varchar(512) NULL COMMENT "公司电子邮箱",
    `sale_income` varchar(64) NULL COMMENT "销售总额",
    `profit_total` varchar(64) NULL COMMENT "利润总额",
    `profit_reta` varchar(64) NULL COMMENT "所有者权益合计",
    `tax_total` varchar(64) NULL COMMENT "纳税总额",
    `total_equity` varchar(64) NULL COMMENT "资产总额",
    `address` varchar(512) NULL COMMENT "地址",
    `fare_scope` varchar(512) NULL COMMENT "主营业务",
    `oper_name` varchar(255) NULL COMMENT "法定代表人",
    `zip_code` varchar(64) NULL COMMENT "邮编",
    `serv_fare_income` varchar(64) NULL COMMENT "主营业务收入",
    `reg_capi` varchar(255) NULL COMMENT "注册资本",
    `indiv_form_mode` varchar(255) NULL COMMENT "江苏地方年报中的法定代表人字段",
    `collegues_num` varchar(64) NULL COMMENT "从业人数",
    `if_website` varchar(64) NULL COMMENT "是否有网站或网店",
    `if_invest` varchar(64) NULL COMMENT "企业是否有投资信息或购买其他公司股权",
    `if_external_guarantee` varchar(64) NULL COMMENT "是否提供对外担保",
    `if_equity` varchar(64) NULL COMMENT "是否发生股东股权转让",
    `create_date` varchar(64) NULL COMMENT "创建时间",
    `status` varchar(64) NULL COMMENT "企业经营状态",
    `fax` varchar(64) NULL COMMENT "传真",
    `json_data` text NULL COMMENT "无业务意义",
    `guarantee_items` text NULL COMMENT "年报担保信息",
    `invest_items` longtext NULL COMMENT "年报投资信息",
    `websites` longtext NULL COMMENT "年报网站信息",
    `external_guarantees` longtext NULL COMMENT "年报对外担保信息",
    `regist_capi_info` text NULL COMMENT "年报注册资本实缴情况",
    `change_records` varchar(64) NULL COMMENT "年报变更记录",
    `branch` text NULL COMMENT "年报分支机构",
    `stock_changes` longtext NULL COMMENT "年报股东变更",
    `invest_situation` varchar(1000) NULL COMMENT "年报出资情况信息",
    `matter_record` text NULL COMMENT "年报备案信息",
    `update_records` longtext NULL COMMENT "年报修改记录",
    `partners` longtext NULL COMMENT "年报股东信息",
    `tel_tags` varchar(64) NULL COMMENT "年报中电话号码隐藏标志",
    `email_tags` varchar(64) NULL COMMENT "年报中邮箱隐藏标志",
    `change_tags` varchar(64) NULL COMMENT "年报修改记录隐藏标志",
    `female_collegues_num` varchar(64) NULL COMMENT "其中女性从业人数",
    `enterprise_holding_situation` varchar(64) NULL COMMENT "企业控股情况",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `sup_ename` varchar(255) NULL COMMENT "隶属企业名称",
    `sup_credit_no` varchar(64) NULL COMMENT "隶属企业统一社会信用代码/注册号",
    `sup_eid` varchar(64) NULL COMMENT "隶属企业eid",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "基本信息-企业年报"'''

    dwd_essential_info_employees_alls = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT NULL COMMENT "加密唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `adm_prim_position` varchar(512) NULL COMMENT "职位",
    `job_title_analy` varchar(1000) NULL COMMENT "解析职位",
    `adm_prim_name` varchar(512) NULL COMMENT "名称",
    `name_type` varchar(64) NULL COMMENT "人员类型",
    `adm_prim_id` varchar(64) NULL COMMENT "pid/eid",
    `sex` varchar(64) NULL COMMENT "性别",
    `u_tags` int NULL COMMENT "是否隐藏",
    `is_history` int NULL COMMENT "是否历史",
    `end_date` date NULL COMMENT "退出时间",
    `is_actual` varchar(64) NULL COMMENT "退出时间是否是实际变更时间",
    `source` varchar(64) NULL COMMENT "数据来源",
    `create_time` varchar(64) NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) NULL COMMENT "数据库行更新时间",
    `start_date` datetime NULL COMMENT "开始任职时间",
    `start_is_actual` varchar(64) NULL COMMENT "任职时间是否是实际变更时间",
    `unq_id` varchar(512) NULL COMMENT "CDC唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "基本信息-企业主要人员"'''

    dwd_essential_info_social_security = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT NULL COMMENT "加密唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `report_year` varchar(64) NULL COMMENT "年报年份",
    `report_name` varchar(64) NULL COMMENT "年报名称",
    `report_date` varchar(64) NULL COMMENT "年报日期",
    `currency` varchar(64) NULL COMMENT "币种",
    `ins_base_display` varchar(64) NULL COMMENT "是否展示单位缴费基数",
    `ins_real_pay_display` varchar(64) NULL COMMENT "是否展示实际缴费金额",
    `ins_unpaid_display` varchar(64) NULL COMMENT "是否展示欠缴金额",
    `ins_num_pen` varchar(64) NULL COMMENT "城镇职工基本养老保险参保人数",
    `ins_base_pen` varchar(64) NULL COMMENT "单位参加城镇职工基本养老保险缴费基数",
    `ins_real_pay_pen` varchar(64) NULL COMMENT "参加城镇职工基本养老保险本期实际缴费金额",
    `ins_unpaid_pen` varchar(64) NULL COMMENT "单位参加城镇职工基本养老保险累计欠缴金额",
    `ins_num_med` varchar(64) NULL COMMENT "职工基本医疗保险人数",
    `ins_base_med` varchar(64) NULL COMMENT "单位参加职工基本医疗保险缴费基数",
    `ins_real_pay_med` varchar(64) NULL COMMENT "参加职工基本医疗保险本期实际缴费金额",
    `ins_unpaid_med` varchar(64) NULL COMMENT "单位参加职工基本医疗保险累计欠缴金额",
    `ins_num_injury` varchar(64) NULL COMMENT "工伤保险人数",
    `ins_base_injury` varchar(64) NULL COMMENT "单位参加工伤保险缴费基数",
    `ins_real_pay_injury` varchar(64) NULL COMMENT "参加工伤保险本期实际缴费金额",
    `ins_unpaid_injury` varchar(64) NULL COMMENT "单位参加工伤保险累计欠缴金额",
    `ins_num_unemp` varchar(64) NULL COMMENT "失业保险人数",
    `ins_base_unemp` varchar(64) NULL COMMENT "单位参加失业保险缴费基数",
    `ins_real_pay_unemp` varchar(64) NULL COMMENT "参加失业保险本期实际缴费金额",
    `ins_unpaid_unemp` varchar(64) NULL COMMENT "单位参加失业保险累计欠缴金额",
    `ins_num_rear` varchar(64) NULL COMMENT "生育保险人数",
    `ins_base_rear` varchar(64) NULL COMMENT "单位参加生育保险缴费基数",
    `ins_real_pay_rear` varchar(64) NULL COMMENT "参加生育保险本期实际缴费金额",
    `ins_unpaid_rear` varchar(64) NULL COMMENT "单位参加生育保险累计欠缴金额",
    `created_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(64) NULL COMMENT "CDC唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "基本信息-社会保险"'''

    dwd_operational_risk_abnor_ope_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `abnor_ope_in_date` date NULL COMMENT "列入日期",
    `abnor_ope_in_agency` varchar(64) NULL COMMENT "列入机关名称",
    `abnor_ope_in_reason` varchar(255) NULL COMMENT "经营异常列入原因",
    `abnor_ope_out_date` date NULL COMMENT "移出日期",
    `abnor_ope_out_agency` varchar(64) NULL COMMENT "移出机关名称",
    `abnor_ope_out_reason` varchar(255) NULL COMMENT "退出异常名录原因",
    `province` varchar(64) NULL COMMENT "省份",
    `is_history` int NULL COMMENT "是否历史",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `abnor_ope_out_date_real` date NULL COMMENT "移出日期原始值",
    `abnor_ope_in_reason_code` varchar(64) NULL COMMENT "经营异常列入原因编码",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-经营异常"'''

    dwd_operational_risk_commercial_adm_penal = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `penal_id` varchar(512) NULL COMMENT "行政处罚记录编号",
    `illegal_type` varchar(64) NULL COMMENT "违法行为类型",
    `illegal_facts` text NULL COMMENT "违法事实",
    `illegal_comment`  text NULL COMMENT "违法行为",
    `penal_reason` varchar(64) NULL COMMENT "处罚事由",
    `penal_basis` text NULL COMMENT "处罚依据",
    `penal_type` varchar(64) NULL COMMENT "处罚种类",
    `penal_type_cn` varchar(512) NULL COMMENT "处罚种类中文",
    `penal_comment`  text NULL COMMENT "处罚内容",
    `admin_penal_agency` varchar(255) NULL COMMENT "处罚机关",
    `penal_validity` varchar(64) NULL COMMENT "处罚有效期",
    `deci_docu_no` varchar(512) NULL COMMENT "处罚决定文书号",
    `deci_docu_content` varchar(64) NULL COMMENT "处罚决定文书内容",
    `deci_agency` varchar(64) NULL COMMENT "决定机关",
    `penal_deci_date` date NULL COMMENT "处罚日期",
    `penal_amt` double NULL COMMENT "处罚金额（万元）",
    `penal_deci_book_date` date NULL COMMENT "处罚决定书签发日期",
    `publicity_date` date NULL COMMENT "公示日期",
    `publicity_deadline` date NULL COMMENT "公示截止期",
    `publicity_done` varchar(64) NULL COMMENT "处罚执行情况",
    `admin_rew_result` varchar(64) NULL COMMENT "行政复议结果",
    `content` text NULL COMMENT "处罚结果",
    `end_date` varchar(64) NULL COMMENT "处罚截止期",
    `status` int NULL COMMENT "当前状态",
    `area` varchar(64) NULL COMMENT "省市",
    `change_infos` text NULL COMMENT "变更情形",
    `is_history` int NULL COMMENT "是否历史",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据变更时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-行政处罚"'''

    dwd_legal_risk_serious_breach_trust_relations_execution = '''CREATE TABLE if not exists %s.%s (
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `is_history` int NULL COMMENT "是否历史",
    `json` text NULL COMMENT "严重违法失信企业名单（黑名单）信息",
    `out_department` varchar(64) NULL COMMENT "移出部门",
    `in_date` varchar(64) NULL COMMENT "列入日期",
    `out_date` varchar(64) NULL COMMENT "移出日期",
    `ill_type` varchar(64) NULL COMMENT "类型",
    `in_department` varchar(64) NULL COMMENT "列入部门",
    `seq_no` int NULL COMMENT "序号",
    `in_reason` varchar(1000) NULL COMMENT "列入原因",
    `out_reason` varchar(255) NULL COMMENT "移出原因",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "nan",
    `is_delete` int NULL COMMENT "nan",
    `md5_id` varchar(64) NOT NULL COMMENT "唯一id(md5_id)",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-严重违法展开"'''

    dwd_legal_risk_serious_breach_trust = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(1000) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `execution` text NULL COMMENT "严重违法失信企业名单（黑名单）信息",
    `executed_person` varchar(64) NULL COMMENT "失信被执行人信息",
    `major_tax_violatio` text NULL COMMENT "重大税收违法案件信息",
    `is_history` int NULL COMMENT "是否历史",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(64) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-严重违法"'''

    dwd_business_info_spot_check = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `seq_no` int NULL COMMENT "序号",
    `name` varchar(255) NULL COMMENT "企业名称",
    `province` varchar(64) NULL COMMENT "省份",
    `check_agency` varchar(255) NULL COMMENT "检查实施机关",
    `check_type` varchar(64) NULL COMMENT "类型",
    `finish_date` varchar(64) NULL COMMENT "检查完成日期 ",
    `check_result` text NULL COMMENT "结果",
    `last_update_time` bigint NULL COMMENT "最后更新时间",
    `u_tags` int NULL COMMENT "是否隐藏",
    `obj_id` varchar(64) NULL COMMENT "mongo数据库中obj_id",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `result_type` int NULL COMMENT "结果类型(result字段的规范化字段)",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-抽查检查"'''

    dwd_business_info_admin_license_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `uniq_check` varchar(64) NULL COMMENT "业务查重字段",
    `seq_no` int NULL COMMENT "序号",
    `license_no` varchar(255) NULL COMMENT "许可文件编号",
    `license_start_date` varchar(64) NULL COMMENT "有效期自",
    `license_end_date` varchar(64) NULL COMMENT "有效期至",
    `license_authority` varchar(512) NULL COMMENT "许可机关",
    `license_file_name` varchar(512) NULL COMMENT "许可文件名称",
    `license_content` text NULL COMMENT "许可内容",
    `license_status` varchar(64) NULL COMMENT "状态",
    `license_status_code` int NULL COMMENT "状态编码",
    `disabled` int NULL COMMENT "是否隐藏",
    `source` varchar(64) NULL COMMENT "数据来源",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `license_date` varchar(64) NULL COMMENT "许可决定日期",
    `data_source` varchar(255) NULL COMMENT "数据来源单位",
    `source_uni_cre_code` varchar(64) NULL COMMENT "数据来源单位统一社会信用代码",
    `deci_docu_no` varchar(255) NULL COMMENT "行政许可决定文书号",
    `license_type` varchar(64) NULL COMMENT "许可类别",
    `auth_usc_code` varchar(64) NULL COMMENT "许可机关统一社会信用代码",
    `license_name` varchar(255) NULL COMMENT "行政许可决定文书名称",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-行政许可"'''

    dwd_legal_risk_executor = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_no` varchar(64) NULL COMMENT "组织机构代码",
    `is_history` int NULL COMMENT "是否历史",
    `title` varchar(64) NULL COMMENT "文书标题",
    `enforce_name` varchar(64) NULL COMMENT "被执行人名称",
    `id_type` varchar(64) NULL COMMENT "被执行人证件类型",
    `id_no` varchar(64) NULL COMMENT "被执行人证件号码",
    `filing_date` date NULL COMMENT "立案日期",
    `case_no` varchar(64) NULL COMMENT "案号",
    `court` varchar(64) NULL COMMENT "执行法院",
    `enforce_amt` decimal(18, 5) NULL COMMENT "执行金额(元)",
    `enforce_money` decimal(18, 5) NULL COMMENT "执行标的(元)",
    `enforce_content` varchar(64) NULL COMMENT "执行内容",
    `enforce_basis_no` varchar(64) NULL COMMENT "执行依据文号",
    `end_date` date NULL COMMENT "终本日期",
    `unfulfilled_amt` decimal(18, 5) NULL COMMENT "未履行金额(元)",
    `apply_person` varchar(64) NULL COMMENT "申请执行人",
    `case_status` varchar(64) NULL COMMENT "案件状态",
    `remark` varchar(64) NULL COMMENT "异议备注",
    `litigation_status` varchar(64) NULL COMMENT "诉讼地位",
    `type` varchar(64) NULL COMMENT "被执行人类型",
    `case_id` varchar(64) NULL COMMENT "案件id",
    `number` varchar(64) NULL COMMENT "企业证照号",
    `court_name` varchar(64) NULL COMMENT "标准法院名称",
    `url` varchar(64) NULL COMMENT "数据源链接",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(64) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-被执行人"'''

    dwd_legal_risk_judicial_docu = '''CREATE TABLE if not exists %s.%s (
    `id` text NULL COMMENT "唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_no` varchar(64) NULL COMMENT "组织机构代码",
    `title` text NULL COMMENT "文书标题",
    `case_no` text NULL COMMENT "案号",
    `litigation_status` varchar(64) NULL COMMENT "诉讼地位",
    `court` varchar(64) NULL COMMENT "执行法院",
    `court_code` varchar(64) NULL COMMENT "法院地区码",
    `posi_nega` varchar(64) NULL COMMENT "正负面",
    `document_type` varchar(64) NULL COMMENT "文书类型",
    `case_reason` varchar(255) NULL COMMENT "案由",
    `standard_case_reason` varchar(64) NULL COMMENT "标准案由",
    `involve_amt` varchar(64) NULL COMMENT "涉案金额(元)/诉讼标的金额",
    `referee_result` text NULL COMMENT "裁判结果",
    `trial_process` varchar(64) NULL COMMENT "审理程序",
    `case_type` varchar(64) NULL COMMENT "案件类型",
    `lawyer` varchar(64) NULL COMMENT "律师",
    `referee_date` varchar(64) NULL COMMENT "裁判日期",
    `release_date` varchar(64) NULL COMMENT "发布日期",
    `standard_role` tinyint NULL COMMENT "标准角色",
    `trail_result` varchar(64) NULL COMMENT "胜败诉",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `sub_amount` decimal(20, 6) NULL COMMENT "请求标的（元）",
    `related_companies` longtext NULL COMMENT "关联企业/个人",
    `related_relation` text NULL COMMENT "关联信息（原告，被告，代理律所等信息）",
    `freezing_info` text NULL COMMENT "冻结信息",
    `year_pubdate` varchar(64) NULL COMMENT "发布年份",
    `case_status` int NULL COMMENT "案件状态",
    `doc_id` varchar(255) NULL COMMENT "文书号",
    `relation_details` text NULL COMMENT "人员信息明细（包含人员，场所，角色等）",
    `case_relation` int NULL COMMENT "是否有案件关联",
    `related_case_no` text NULL COMMENT "关联案号",
    `initial_court_code` varchar(64) NULL COMMENT "法院码",
    `source` varchar(64) NULL COMMENT "来源",
    `is_history` int NULL COMMENT "是否历史",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-裁判文书"'''

    dwd_legal_risk_court_notice = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `announce_type` varchar(64) NULL COMMENT "公告类型",
    `announce_content` text NULL COMMENT "公告内容",
    `release_date` date NULL COMMENT "发布日期",
    `court` varchar(64) NULL COMMENT "法院",
    `case_no` text NULL COMMENT "案号",
    `case_reason` varchar(64) NULL COMMENT "案由",
    `standard_case_reason` varchar(64) NULL COMMENT "标准案由",
    `announce_people` varchar(64) NULL COMMENT "公告人",
    `published_date` date NULL COMMENT "刊登日期",
    `litigation_status` varchar(64) NULL COMMENT "诉讼地位",
    `people` text NULL COMMENT "当事人",
    `related_companies` longtext NULL COMMENT "关联企业和个人",
    `relation_details` text NULL COMMENT "关联企业/个人详细信息",
    `related_check` text NULL COMMENT "历史关联企业/个人和 当前关联企业/个人数据",
    `url` varchar(255) NULL COMMENT "详情页链接",
    `is_history` int NULL COMMENT "是否历史记录",
    `role` int NULL COMMENT "当事人身份",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-法院公告"'''

    dwd_legal_risk_frozen_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_no` varchar(64) NULL COMMENT "组织机构代码",
    `freeze_detail_freeze_item` varchar(512) NULL COMMENT "冻结执行事项",
    `freeze_detail_freeze_id_no` varchar(64) NULL COMMENT "冻结被执行人证件号码",
    `freeze_detail_freeze_equity_firm` varchar(64) NULL COMMENT "冻结股权标的企业",
    `freeze_detail_freeze_amt` varchar(64) NULL COMMENT "冻结金额（元）",
    `freeze_detail_freeze_agency` varchar(64) NULL COMMENT "冻结机关",
    `litigation_status` varchar(64) NULL COMMENT "诉讼地位",
    `executive_court` varchar(255) NULL COMMENT "执行法院",
    `is_history` int NULL COMMENT "是否历史",
    `frozen_share_status` int NULL COMMENT "股权当前状态",
    `lose_efficacy_reason` varchar(64) NULL COMMENT "失效原因",
    `lose_efficacy_date` date NULL COMMENT "失效时间",
    `frozen_share_type` int NULL COMMENT "类型编码",
    `freeze_detail_freeze_notice_no` varchar(255) NULL COMMENT "冻结执行通知书文号",
    `freeze_detail_freeze_public_date` date NULL COMMENT "冻结公示日期",
    `freeze_detail_freeze_id_type` varchar(64) NULL COMMENT "冻结被执行人证件种类",
    `freeze_detail_freeze_enforce_name` varchar(255) NULL COMMENT "冻结被执行人名称",
    `freeze_detail_freeze_equity_amt` varchar(64) NULL COMMENT "冻结被执行人持有股权、其它投资权益的数额",
    `freeze_detail_freeze_equity_amt_unit` varchar(64) NULL COMMENT "冻结被执行人持有股权、其它投资权益数额的单位",
    `freeze_detail_freeze_start_date` date NULL COMMENT "冻结日期自",
    `freeze_detail_freeze_end_date` date NULL COMMENT "冻结日期至",
    `freeze_detail_freeze_peroid` varchar(64) NULL COMMENT "冻结期限",
    `freeze_detail_freeze_court` varchar(255) NULL COMMENT "冻结执行法院",
    `freeze_detail_freeze_adjudicate_no` varchar(255) NULL COMMENT "冻结执行裁定书文号",
    `fre_eid` varchar(64) NULL COMMENT "被执行人eid",
    `source_eid` varchar(64) NULL COMMENT "第三方eid",
    `detail_eid` varchar(64) NULL COMMENT "冻结详情中的被执行人eid",
    `detail_assist_ident_no` varchar(64) NULL COMMENT "冻结详情中的被执行企业证照号",
    `pc_freeze_detail` varchar(1000) NULL COMMENT "股东变更登记情况",
    `un_freeze_details` text NULL COMMENT "股权冻结-解冻详情",
    `continue_freeze_details` text NULL COMMENT "续行冻结详情",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-股权冻结"'''

    dwd_legal_risk_court_session_announcement = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `is_history` int NULL COMMENT "是否历史",
    `court_date` datetime NULL COMMENT "开庭日期",
    `court_time` datetime NULL COMMENT "开庭时间",
    `case_no` varchar(512) NULL COMMENT "案号",
    `standard_case_reason` varchar(64) NULL COMMENT "标准案由代码",
    `undertake_department` varchar(255) NULL COMMENT "承办部门",
    `tribunal` varchar(512) NULL COMMENT "法庭",
    `partys` text NULL COMMENT "当事人",
    `area` varchar(64) NULL COMMENT "地区",
    `schedule_date` datetime NULL COMMENT "排期日期",
    `presiding_judge` varchar(512) NULL COMMENT "审判长/主审人",
    `court` varchar(64) NULL COMMENT "法院",
    `court_code` varchar(64) NULL COMMENT "法院地区码",
    `announce_content` text NULL COMMENT "公告内容",
    `title` varchar(512) NULL COMMENT "标题",
    `url` text NULL COMMENT "数据源链接",
    `pure_role` varchar(64) NULL COMMENT "当事人身份",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `case_reason` varchar(255) NULL COMMENT "标准案由",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-开庭公告"'''

    dwd_legal_risk_registration_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(1000) NULL COMMENT "唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_no` varchar(64) NULL COMMENT "组织机构代码",
    `case_no` varchar(1000) NULL COMMENT "案号",
    `is_standard_case_no` tinyint NULL COMMENT "是否标准案号",
    `filing_date` varchar(64) NULL COMMENT "立案日期",
    `case_reason` varchar(255) NULL COMMENT "案由",
    `standard_case_reason` varchar(64) NULL COMMENT "标准案由",
    `court_date` varchar(64) NULL COMMENT "开庭日期",
    `end_date` varchar(64) NULL COMMENT "结束日期",
    `undertake_judge` varchar(64) NULL COMMENT "承办法官",
    `assistant_judge` varchar(64) NULL COMMENT "法官助理",
    `undertake_department` varchar(64) NULL COMMENT "承办部门",
    `court` varchar(64) NULL COMMENT "法院",
    `lawer` varchar(64) NULL COMMENT "律师",
    `region` varchar(64) NULL COMMENT "地区",
    `case_type` varchar(64) NULL COMMENT "案件类型",
    `case_status` int NULL COMMENT "案件状态",
    `clean_role` varchar(64) NULL COMMENT "角色",
    `standard_role` tinyint NULL COMMENT "标准角色",
    `role_type` varchar(64) NULL COMMENT "角色类型",
    `related_roles` longtext NULL COMMENT "关联角色",
    `amount` decimal(20, 6) NULL COMMENT "标的金额（元）",
    `url` varchar(1000) NULL COMMENT "数据源链接",
    `is_history` int NULL COMMENT "是否历史记录",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-立案信息"'''

    dwd_legal_risk_dishonest_executor = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_no` varchar(64) NULL COMMENT "组织机构代码",
    `is_history` int NULL COMMENT "是否历史",
    `title` varchar(64) NULL COMMENT "文书标题",
    `filing_date` date NULL COMMENT "立案日期",
    `case_no` varchar(64) NULL COMMENT "案号",
    `court` varchar(64) NULL COMMENT "执行法院",
    `enforce_basis_no` varchar(255) NULL COMMENT "执行依据文号",
    `end_date` date NULL COMMENT "终本日期",
    `province` varchar(64) NULL COMMENT "省份",
    `atten_num` int NULL COMMENT "关注次数",
    `release_date` date NULL COMMENT "发布日期",
    `involve_amt` decimal(18, 5) NULL COMMENT "涉案金额(元)",
    `enforce_content` varchar(64) NULL COMMENT "执行内容",
    `case_status` int NULL COMMENT "案件状态",
    `remark` varchar(64) NULL COMMENT "异议备注",
    `enforce_type` varchar(64) NULL COMMENT "被执行人类型",
    `enforce_name` varchar(64) NULL COMMENT "被执行人名称",
    `id_type` varchar(64) NULL COMMENT "被执行人证件类型",
    `id_no` varchar(64) NULL COMMENT "被执行人证件号码",
    `fulfilled_amt` decimal(18, 5) NULL COMMENT "已履行金额(元)",
    `unfulfilled_amt` decimal(18, 5) NULL COMMENT "未履行金额(元)",
    `perf_details` varchar(64) NULL COMMENT "被执行人履行情况",
    `basis_agency` varchar(255) NULL COMMENT "做出执行依据单位",
    `legal_obligations` text NULL COMMENT "生效法律文书确定的义务",
    `default_beha_details` varchar(255) NULL COMMENT "失信被执行人行为具体情形",
    `apply_person` varchar(64) NULL COMMENT "申请执行人",
    `enforcer_age` varchar(64) NULL COMMENT "被执行人年龄",
    `enforcer_sex` varchar(64) NULL COMMENT "被执行人性别",
    `idissue_place` varchar(64) NULL COMMENT "身份证原始发证地",
    `litigation_status` varchar(64) NULL COMMENT "诉讼地位",
    `name` varchar(64) NULL COMMENT "当事人名称",
    `oper_name` varchar(64) NULL COMMENT "法定代表人",
    `number` varchar(64) NULL COMMENT "企业证照号",
    `type` varchar(64) NULL COMMENT "当事人类型",
    `relatives` varchar(255) NULL COMMENT "关联企业/个人",
    `related_companies` varchar(255) NULL COMMENT "关联企业",
    `amount` double NULL COMMENT "标的",
    `source` varchar(64) NULL COMMENT "数据来源",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(64) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-失信被执行人"'''

    dwd_legal_risk_judicial_auction = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `auction_date` text NULL COMMENT "拍卖日期",
    `auction_object` text NULL COMMENT "拍卖标的",
    `start_price` varchar(64) NULL COMMENT "起拍价",
    `dis_court` varchar(64) NULL COMMENT "处置法院",
    `right_source` varchar(64) NULL COMMENT "权利来源",
    `right_limit` text NULL COMMENT "权利限制",
    `description` text NULL COMMENT "拍品介绍",
    `valuation` varchar(64) NULL COMMENT "评估价",
    `deal_result` varchar(64) NULL COMMENT "成交结果",
    `auction_owner` varchar(512) NULL COMMENT "拍品所有人",
    `warrant_condition` varchar(512) NULL COMMENT "权证情况",
    `provide_document` text NULL COMMENT "提供文件",
    `category` tinyint NULL COMMENT "拍品类别",
    `area` varchar(64) NULL COMMENT "所在地",
    `related_companies` text NULL COMMENT "关联企业/个人",
    `content` text NULL COMMENT "内容",
    `name` varchar(512) NULL COMMENT "项目名称",
    `url` varchar(255) NULL COMMENT "拍卖信息链接",
    `transaction_date` varchar(64) NULL COMMENT "成交日期",
    `transaction_price` varchar(64) NULL COMMENT "成交价格",
    `start_date` varchar(64) NULL COMMENT "拍卖开始时间",
    `end_date` varchar(64) NULL COMMENT "拍卖结束时间",
    `is_history` int NULL COMMENT "是否历史记录",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-司法拍卖"'''

    dwd_legal_risk_high_consum_limit = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `filing_date` date NULL COMMENT "立案日期",
    `enforce_name` varchar(64) NULL COMMENT "被执行人名称",
    `case_no` varchar(64) NULL COMMENT "案号",
    `standard_case_reason` varchar(64) NULL COMMENT "标准案由",
    `court` varchar(64) NULL COMMENT "执行法院",
    `enforce_content` text NULL COMMENT "执行内容",
    `issue_date` date NULL COMMENT "限制令发布日期",
    `apply_person` varchar(512) NULL COMMENT "申请执行人",
    `company_name` varchar(64) NULL COMMENT "关联企业名称",
    `company_name_eid` varchar(64) NULL COMMENT "关联企业eid",
    `sex` varchar(64) NULL COMMENT "性别",
    `court_code` varchar(64) NULL COMMENT "执行法院区域码",
    `execution_applicant` varchar(64) NULL COMMENT "申请执行人eid",
    `source` varchar(255) NULL COMMENT "数据来源",
    `pdf_path` varchar(255) NULL COMMENT "原文pdf链接，指上传服务器后的链接",
    `html_path` varchar(255) NULL COMMENT "原文pdf链接，指上传服务器后的链接 相应HTML链接",
    `case_relation` int NULL COMMENT "案件关联要求字段",
    `is_history` int NULL COMMENT "是否历史记录",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(64) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-限制高消费"'''

    dwd_legal_risk_final_case_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `is_history` int NULL COMMENT "是否历史",
    `enforce_name` varchar(64) NULL COMMENT "被执行人名称",
    `id_type` varchar(64) NULL COMMENT "被执行人证件种类",
    `id_no` varchar(64) NULL COMMENT "被执行人证件号码",
    `title` varchar(64) NULL COMMENT "文书标题",
    `filing_date` date NULL COMMENT "立案日期",
    `court` varchar(64) NULL COMMENT "法院",
    `enforce_money` decimal(18, 5) NULL COMMENT "执行标的(元)",
    `enforce_content` varchar(64) NULL COMMENT "执行内容",
    `case_status` varchar(64) NULL COMMENT "案件状态",
    `remark` varchar(64) NULL COMMENT "异议备注",
    `enforce_basis_no` varchar(64) NULL COMMENT "执行依据文号",
    `end_date` date NULL COMMENT "终本日期",
    `unfulfilled_amt` decimal(18, 5) NULL COMMENT "未履行金额(元)",
    `apply_person` varchar(64) NULL COMMENT "申请执行人",
    `litigation_status` varchar(64) NULL COMMENT "诉讼地位",
    `address` varchar(255) NULL COMMENT "地址",
    `card_num` varchar(64) NULL COMMENT "企业证照号",
    `sex` int NULL COMMENT "性别",
    `case_no_terminal` varchar(64) NULL COMMENT "终本案号",
    `case_no_origin` varchar(255) NULL COMMENT "原始案号(立案案号)",
    `case_relation` int NULL COMMENT "案件关联要求字段",
    `status` int NULL COMMENT "履行状态",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "司法涉诉-终本信息"'''

    dwd_essential_info_investment_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `inv_firm_name` varchar(64) NULL COMMENT "被投资公司名称",
    `inv_firm_id` varchar(64) NULL COMMENT "被投资公司id",
    `inv_firm_type` varchar(64) NULL COMMENT "被投资公司类型",
    `inv_firm_ope_status` int NULL COMMENT "被投资公司经营状态",
    `inv_ratio` decimal(18, 5) NULL COMMENT "投资比例",
    `should_cap` decimal(18, 5) NULL COMMENT "认缴出资额",
    `should_cap_date` date NULL COMMENT "认缴出资日期",
    `should_cap_ratio` decimal(18, 5) NULL COMMENT "认缴出资比例",
    `should_cap_mode` varchar(64) NULL COMMENT "认缴出资方式",
    `should_cap_cur` varchar(64) NULL COMMENT "认缴出资币种",
    `should_cap_conv` varchar(64) NULL COMMENT "认缴出资额（人民币）",
    `cumulative_amt` decimal(18, 5) NULL COMMENT "累计认缴额",
    `cumulative_amt_cur` varchar(64) NULL COMMENT "累计认缴币种",
    `real_cap` decimal(18, 5) NULL COMMENT "实缴出资额",
    `real_cap_date` date NULL COMMENT "实缴出资日期",
    `real_cap_ratio` decimal(18, 5) NULL COMMENT "实缴出资比例",
    `real_cap_mode` varchar(64) NULL COMMENT "实缴出资方式",
    `real_cap_cur` varchar(64) NULL COMMENT "实缴出资币种",
    `acc_real_cap` decimal(18, 5) NULL COMMENT "累计实缴额",
    `acc_real_cap_cur` varchar(64) NULL COMMENT "累计实缴币种",
    `inv_firm_quote_status` int NULL COMMENT "被投资公司上市状态",
    `is_history` int NULL COMMENT "是否历史",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "投融资-对外投资"'''

    dwd_operational_risk_equity_pledge_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `pledge_no` varchar(64) NULL COMMENT "股权出质登记编号",
    `pledgor_name` varchar(255) NULL COMMENT "出质人名称",
    `pledgor_type` varchar(64) NULL COMMENT "出质人类型",
    `pledgor_id` varchar(255) NULL COMMENT "出质人id",
    `pledge_amt` decimal(18, 5) NULL COMMENT "出质股权数额",
    `pledge_amt_unit` varchar(64) NULL COMMENT "出质股权单位",
    `pledge_amt_cur` varchar(64) NULL COMMENT "出质股权币种",
    `pledgee_name` varchar(255) NULL COMMENT "质权人名称",
    `pledgee_type` varchar(64) NULL COMMENT "质权人类型",
    `pledgee_id` varchar(512) NULL COMMENT "质权人id",
    `pledge_reg_date` date NULL COMMENT "股权出质登记日期",
    `pledge_status` int NULL COMMENT "股权出质状态",
    `object_company_name` varchar(64) NULL COMMENT "标的企业名称",
    `object_company_id` varchar(64) NULL COMMENT "标的企业id",
    `public_date` date NULL COMMENT "公示日期",
    `change_items` varchar(1000) NULL COMMENT "变更信息",
    `cancel_items` varchar(255) NULL COMMENT "注销信息",
    `cancel_date` date NULL COMMENT "注销日期",
    `cancel_content` int NULL COMMENT "注销原因",
    `is_history` int NULL COMMENT "是否历史",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `pledgor_id_no` varchar(64) NULL COMMENT "出质企业证照号",
    `pledgee_id_no` varchar(64) NULL COMMENT "质权企业证照号",
    `role` int NULL COMMENT "角色",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "投融资-股权出质"'''

    dwd_intellectual_property_trademark_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `trademark_name` varchar(512) NULL COMMENT "商标名称",
    `trademark_status` int NULL COMMENT "商标状态",
    `trademark_apply_date` date NULL COMMENT "商标申请日期",
    `trademark_reg_no` varchar(64) NULL COMMENT "商标注册号",
    `international_classification` varchar(64) NULL COMMENT "国际分类",
    `trademark_applicat_name` varchar(64) NULL COMMENT "商标申请人名称",
    `trademark_applicat_addr` varchar(255) NULL COMMENT "商标申请人地址",
    `trademark_applicat_name_en` varchar(255) NULL COMMENT "商标申请人英文名称",
    `trademark_applicat_addr_en` varchar(255) NULL COMMENT "商标申请人英文地址",
    `trademark_first_trial_no` varchar(64) NULL COMMENT "商标初审公告期号",
    `trademark_first_trial_date` date NULL COMMENT "商标初审公告日期",
    `trademark_reg_period_no` varchar(64) NULL COMMENT "商标注册公告期号",
    `trademark_reg_date` varchar(64) NULL COMMENT "商标注册公告日期",
    `trademark_period` varchar(64) NULL COMMENT "商标专用期限",
    `privileges_start_date` date NULL COMMENT "开始日期(专用权期限)",
    `privileges_end_date` date NULL COMMENT "结束日期(专用权期限)",
    `trademark_agency` varchar(64) NULL COMMENT "商标代理机构信息",
    `is_shared` varchar(64) NULL COMMENT "是否共有商标",
    `shared_person_name` varchar(255) NULL COMMENT "共有申请人名称",
    `trademark_type` int NULL COMMENT "商标类型",
    `international_reg_date` varchar(64) NULL COMMENT "国际注册日期",
    `later_designated_date` varchar(64) NULL COMMENT "后期指定日期",
    `priority_date` varchar(64) NULL COMMENT "优先权日期",
    `trademark_image` varchar(255) NULL COMMENT "商标图片地址",
    `trademark_process_status` longtext NULL COMMENT "商标流程状态",
    `trademark_announce` longtext NULL COMMENT "商标公告",
    `trademark_products_name` longtext NULL COMMENT "商标产品服务列表名",
    `is_history` int NULL COMMENT "是否历史",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `trademark_form` varchar(64) NULL COMMENT "nan",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "知识产权-商标信息"'''

    dwd_intellectual_property_copyright = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `copyright_id` varchar(64) NULL COMMENT "著作id",
    `copyright_reg_no` varchar(64) NULL COMMENT "著作登记号",
    `copyright_reg_type` varchar(64) NULL COMMENT "著作登记类别",
    `copyright_firm` varchar(255) NULL COMMENT "著作持有企业",
    `copyright_version` varchar(64) NULL COMMENT "著作版本号",
    `copyright_name` varchar(512) NULL COMMENT "著作名称",
    `copyright_finish_date` varchar(64) NULL COMMENT "著作完成日期",
    `copyright_approval_date` date NULL COMMENT "著作批准日期",
    `copyright_related_companies` text NULL COMMENT "著作相关公司",
    `copyright_short_name` varchar(255) NULL COMMENT "著作简称",
    `copyright_last_alt_date` date NULL COMMENT "著作最后更新日期",
    `copyright_publish_date` date NULL COMMENT "著作首次发表日期",
    `copyright_reg_date` date NULL COMMENT "著作登记日期",
    `copyright_img_url` varchar(64) NULL COMMENT "作品图片链接",
    `copyright_type_no` varchar(64) NULL COMMENT "著作分类号",
    `copyright_release_date` date NULL COMMENT "著作发布日期",
    `state` int NULL COMMENT "状态",
    `agency` varchar(64) NULL COMMENT "代理机构",
    `agent` varchar(64) NULL COMMENT "代理人",
    `process` varchar(64) NULL COMMENT "流程状态",
    `products` varchar(64) NULL COMMENT "服务项目/商品",
    `author` varchar(255) NULL COMMENT "作者",
    `copyright_type` varchar(64) NULL COMMENT "作品类别",
    `is_history` int NULL COMMENT "是否历史",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "知识产权-著作权"'''

    dwd_intellectual_property_patent_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `patent_name` varchar(255) NULL COMMENT "专利名称",
    `patent_type` varchar(64) NULL COMMENT "专利类型",
    `patent_apply_date` date NULL COMMENT "专利申请日期",
    `patent_apply_no` varchar(64) NULL COMMENT "专利申请号",
    `patent_no` varchar(64) NULL COMMENT "专利号",
    `patent_grant_date` date NULL COMMENT "授予日期",
    `patent_period` varchar(64) NULL COMMENT "专利有效期",
    `paten_public_date` date NULL COMMENT "专利授权公开日",
    `paten_public_no` varchar(64) NULL COMMENT "专利授权公开号",
    `last_legal_status` varchar(64) NULL COMMENT "最新法律状态",
    `priority_no` varchar(64) NULL COMMENT "优先权号",
    `priority_date` date NULL COMMENT "优先权日",
    `applicat_name` varchar(512) NULL COMMENT "申请人",
    `inventor` varchar(512) NULL COMMENT "发明人",
    `agency` varchar(64) NULL COMMENT "代理机构",
    `agency_eid` varchar(64) NULL COMMENT "代理机构eid",
    `agent` varchar(255) NULL COMMENT "代理人",
    `ipcsort_no` text NULL COMMENT "IPC分类号",
    `cpcsort_no` varchar(64) NULL COMMENT "CPC分类号",
    `applicat_addr` varchar(255) NULL COMMENT "申请人地址",
    `applicat_postcode` varchar(64) NULL COMMENT "申请人邮编",
    `summary` text NULL COMMENT "摘要",
    `summary_img` text NULL COMMENT "摘要附图",
    `legal_status` longtext NULL COMMENT "法律状态",
    `rights_request` varchar(64) NULL COMMENT "权利要求",
    `description_book` varchar(64) NULL COMMENT "说明书",
    `patent_role` varchar(64) NULL COMMENT "专利角色",
    `related_companies` text NULL COMMENT "关联企业",
    `pct_req_num` varchar(64) NULL COMMENT "PCT申请号",
    `pct_req_date` varchar(64) NULL COMMENT "PCT申请日期",
    `pct_req_data` varchar(64) NULL COMMENT "PCT申请数据",
    `pct_pub_num` varchar(64) NULL COMMENT "PCT公布号",
    `pct_pub_date` varchar(64) NULL COMMENT "PCT公布日期",
    `pct_pub_data` varchar(255) NULL COMMENT "PCT公布数据",
    `pct_date` varchar(64) NULL COMMENT "PCT进入国家阶段日",
    `pct_apply_country` varchar(64) NULL COMMENT "申请国家",
    `patent_person_ori` varchar(255) NULL COMMENT "原始专利权人",
    `patent_person` varchar(512) NULL COMMENT "当前申请（专利权）人",
    `authorize_tag` int NULL COMMENT "授权标志",
    `annex` varchar(255) NULL COMMENT "附件",
    `is_history` int NULL COMMENT "是否历史",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `priority` text NULL COMMENT "优先权",
    `priority_local` text NULL COMMENT "本国优先权",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "知识产权-专利信息"'''

    dwd_operational_risk_mort_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `mort_no` varchar(64) NULL COMMENT "登记编号",
    `mort_date` date NULL COMMENT "登记日期",
    `mort_institution` varchar(64) NULL COMMENT "登记机关",
    `mort_amt` decimal(18, 5) NULL COMMENT "被担保债权数额",
    `mort_currency` varchar(64) NULL COMMENT "债权单位",
    `mort_status` int NULL COMMENT "动产抵押状态 ",
    `mort_pub_date` date NULL COMMENT "公示日期",
    `mort_cancel_date` date NULL COMMENT "注销日期",
    `mort_cancel_reason` varchar(512) NULL COMMENT "注销原因",
    `mabguar_range` varchar(1000) NULL COMMENT "担保范围",
    `mabguar_type_code` int NULL COMMENT "被担保债券种类代码",
    `mabguar_type` varchar(255) NULL COMMENT "被担保债券种类",
    `period_start_date` date NULL COMMENT "抵押起始时间",
    `period_end_date` date NULL COMMENT "抵押结束时间",
    `mort_alt_content` text NULL COMMENT "变更内容",
    `guarantees` longtext NULL COMMENT "抵押物信息",
    `mortgagees` text NULL COMMENT "抵押人信息",
    `remark` varchar(1000) NULL COMMENT "备注",
    `is_history` int NULL COMMENT "是否历史",
    `unq_id` varchar(255) NULL COMMENT "CDC唯一id",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `mort_period` varchar(64) NULL COMMENT "抵押期限",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "投融资-动产抵押"'''

    dwd_operational_risk_clear_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `liquidation_oligor` varchar(64) NULL COMMENT "清算义务人",
    `liquidation_principal` varchar(255) NULL COMMENT "清算组负责人",
    `liquidation_member` varchar(1000) NULL COMMENT "清算组成员",
    `liquidation_addr` varchar(64) NULL COMMENT "清算组办公地址",
    `liquidation_tel` varchar(64) NULL COMMENT "清算组联系电话",
    `liquidation_reg_date` date NULL COMMENT "清算组备案日期",
    `liquidation_est_date` date NULL COMMENT "清算组成立日期",
    `liquidation_end_details` varchar(64) NULL COMMENT "清算完结情况",
    `liquidation_start_date` date NULL COMMENT "清算开始日期",
    `liquidation_end_date` date NULL COMMENT "清算完结日期",
    `liquidation_period` varchar(64) NULL COMMENT "清算时长",
    `liquidation_type` varchar(64) NULL COMMENT "清算类型",
    `status` int NULL COMMENT "状态",
    `liquidation_prop_amt` decimal(18, 5) NULL COMMENT "清算财产数额",
    `liquidation_value_method` varchar(64) NULL COMMENT "清算财产计价方法",
    `liquidation_cost` decimal(18, 5) NULL COMMENT "清算费用",
    `frozen_firm_name` varchar(64) NULL COMMENT "股权冻结企业名称",
    `liquidation_debt_receiver` varchar(64) NULL COMMENT "债务承接人",
    `liquidation_credit_receiver` varchar(64) NULL COMMENT "债权承接人",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-清算信息"'''

    dwd_operational_risk_simple_cancel_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id ",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id) ",
    `firm_eid` varchar(64) NULL COMMENT "企业eid ",
    `firm_id` varchar(64) NULL COMMENT "企业id ",
    `firm_name` varchar(64) NULL COMMENT "企业名称 ",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码 ",
    `reg_no` varchar(64) NULL COMMENT "注册号 ",
    `org_code` varchar(64) NULL COMMENT "组织机构代码 ",
    `registra_agency` varchar(64) NULL COMMENT "登记机关 ",
    `commit_of_all_invest` varchar(512) NULL COMMENT "全体投资人承诺书（地址） ",
    `announce_applica_date` varchar(64) NULL COMMENT "公告申请日期 ",
    `announce_period` varchar(64) NULL COMMENT "公告期限 ",
    `announce_start_date` varchar(64) NULL COMMENT "公告开始日期 ",
    `announce_end_date` varchar(64) NULL COMMENT "公告结束日期 ",
    `objection_info` text NULL COMMENT "异议信息 ",
    `objection_applicat` varchar(64) NULL COMMENT "异议申请人 ",
    `objection_date` date NULL COMMENT "异议时间 ",
    `objection_content` varchar(64) NULL COMMENT "异议内容 ",
    `simple_cancel_result` varchar(255) NULL COMMENT "简易注销结果 ",
    `result` varchar(64) NULL COMMENT "结果 ",
    `create_time` datetime NULL COMMENT "创建时间 ",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "本地数据库行更新时间 ",
    `is_history` int NULL COMMENT "是否历史 ",
    `is_delete` int NULL COMMENT "是否删除 ",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-简易注销信息"'''

    dwd_operational_info_double_random_check = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(1000) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `plan_no` varchar(64) NULL COMMENT "抽查计划编号",
    `plan_name` varchar(255) NULL COMMENT "抽查计划名称",
    `task_no` varchar(64) NULL COMMENT "抽查任务编号",
    `task_name` varchar(255) NULL COMMENT "抽查任务名称",
    `check_type` int NULL COMMENT "抽查类型",
    `check_organ` varchar(1000) NULL COMMENT "抽查机关",
    `finish_date` varchar(64) NULL COMMENT "抽查完成日期",
    `details` longtext NULL COMMENT "详情列表",
    `check_result` varchar(64) NULL COMMENT "抽查结果",
    `create_time` date NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-双随机抽检"'''

    dwd_tax_info_taxarrears_info = '''CREATE TABLE if not exists %s.%s (
    `_id` varchar(64) NULL COMMENT "关联id",
    `id` varchar(512) NULL COMMENT "业务唯一主键",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `tax_owe_id` varchar(64) NULL COMMENT "欠税记录编号",
    `tax_owe_date` varchar(64) NULL COMMENT "欠税时间",
    `tax_payer_type` varchar(64) NULL COMMENT "纳税人类型",
    `tax_payer_no` varchar(64) NULL COMMENT "纳税人识别号",
    `tax_payer_name` varchar(255) NULL COMMENT "纳税人名称",
    `tax_begin` datetime NULL COMMENT "所属时期起",
    `tax_end` datetime NULL COMMENT "所属时期止",
    `tax_owe_term` varchar(64) NULL COMMENT "征收品目",
    `tax_type` varchar(512) NULL COMMENT "税款种类",
    `tax_basis` decimal(18, 5) NULL COMMENT "计税依据",
    `tax_rate` decimal(18, 5) NULL COMMENT "税率",
    `tax_owe_amt` decimal(18, 5) NULL COMMENT "欠税金额(元)",
    `tax_owe_balance` decimal(18, 5) NULL COMMENT "欠税余额",
    `tax_owe_record_date` datetime NULL COMMENT "欠税统计时间",
    `tax_status` varchar(64) NULL COMMENT "应税税款状态",
    `reduce_tax_amt` decimal(18, 5) NULL COMMENT "应补退税额",
    `payment_period` datetime NULL COMMENT "缴纳期限",
    `tax_admin_agency` varchar(64) NULL COMMENT "主管税务机关",
    `tax_issue_agency` varchar(64) NULL COMMENT "发布单位",
    `assert_date` datetime NULL COMMENT "认定日期",
    `assert_agency` varchar(64) NULL COMMENT "认定单位",
    `assert_agency_address` varchar(64) NULL COMMENT "认定单位的地址",
    `assert_agency_tel` varchar(64) NULL COMMENT "认定单位电话",
    `assert_reason` varchar(64) NULL COMMENT "认定原因",
    `legal_rep` varchar(64) NULL COMMENT "法定代表人姓名",
    `ope_address` varchar(255) NULL COMMENT "经营地址",
    `is_history` int NULL COMMENT "是否隐藏",
    `created_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `unq_id` varchar(64) NULL COMMENT "cdc唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-欠税信息"'''

    dwd_essential_info_used_name = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(255) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `history_firm_name` varchar(512) NULL COMMENT "历史企业名称",
    `format_history_firm_name` varchar(255) NULL COMMENT "历史企业名称(清洗后)",
    `start_date` varchar(64) NULL COMMENT "历史名称开始时间",
    `is_actual_start_date` varchar(64) NULL COMMENT "否是实际开始时间",
    `alt_date` varchar(64) NULL COMMENT "变更时间",
    `is_actual_alt_date` varchar(64) NULL COMMENT "是否实际变更时间",
    `source` varchar(64) NULL COMMENT "数据来源",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    `ope_status` varchar(64) NULL COMMENT "经营状态",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "基本信息-曾用名"'''

    dwd_operational_risk_major_illegal_tax_cases = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `taxpayer_name` varchar(64) NULL COMMENT "纳税人名称",
    `taxpayer_no` varchar(64) NULL COMMENT "纳税人识别号",
    `case_nature` varchar(255) NULL COMMENT "案件性质",
    `illegal_facts` varchar(1000) NULL COMMENT "主要违法事实",
    `punish_status` text NULL COMMENT "处罚依据及税务处理处罚情况",
    `start_date` varchar(64) NULL COMMENT "违法事实中开始时间",
    `end_date` varchar(64) NULL COMMENT "违法事实中结束时间",
    `release_date` varchar(64) NULL COMMENT "发布日期",
    `check_agency` varchar(64) NULL COMMENT "检查机关",
    `tax_admin_agency` varchar(64) NULL COMMENT "所属税务机关",
    `tax_public_agency` varchar(64) NULL COMMENT "公示税务机关",
    `police` varchar(64) NULL COMMENT "移送公安情况",
    `leg_info` varchar(255) NULL COMMENT "法定代表人或负责人信息",
    `leg_name` varchar(64) NULL COMMENT "法定代表人或负责人姓名",
    `leg_sex` varchar(64) NULL COMMENT "法定代表人或负责人性别",
    `leg_id_type` varchar(64) NULL COMMENT "法定代表人或者负责人证件类型",
    `leg_id_no` varchar(255) NULL COMMENT "法定代表人或负责人证件号码",
    `fin_info` varchar(255) NULL COMMENT "负有直接责任的财务负责信息",
    `fin_name` varchar(255) NULL COMMENT "负有直接责任的财务负责人姓名",
    `fin_sex` varchar(64) NULL COMMENT "负有直接责任的财务负责人性别",
    `fin_id_type` varchar(64) NULL COMMENT "负有直接责任的财务负责人证件类型",
    `fin_id_no` varchar(64) NULL COMMENT "负有直接责任的财务负责人证件号码",
    `agency_info` varchar(255) NULL COMMENT "负有直接责任的中介机构信息及其从业人员信息",
    `agency_name` varchar(64) NULL COMMENT "负有直接责任的中介机构名称",
    `agency_staff_name` varchar(64) NULL COMMENT "负有直接责任的中介机构从业人员姓名",
    `agency_staff_sex` varchar(64) NULL COMMENT "负有直接责任的中介机构从业人员性别",
    `agency_staff_id_type` varchar(64) NULL COMMENT "负有直接责任的中介机构从业人员证件类型",
    `agency_staff_id_no` varchar(64) NULL COMMENT "负有直接责任的中介机构从业人员证件号码",
    `illegal_period_leg_info` varchar(512) NULL COMMENT "违法期间法人代表或者负责人信息",
    `illegal_period_leg_name` varchar(255) NULL COMMENT "违法期间法人代表或者负责人姓名",
    `illegal_period_leg_sex` varchar(64) NULL COMMENT "违法期间法人代表或者负责人姓别",
    `illegal_period_leg_id_type` varchar(64) NULL COMMENT "违法期间法人代表或者负责人证件类型",
    `illegal_period_leg_id_no` varchar(255) NULL COMMENT "违法期间法人代表或者负责人证件号码",
    `actual_leg_info` varchar(255) NULL COMMENT "实际负责人信息",
    `actual_leg_name` varchar(64) NULL COMMENT "实际负责人姓名",
    `actual_leg_sex` varchar(64) NULL COMMENT "实际负责人姓别",
    `actual_leg_id_type` varchar(64) NULL COMMENT "实际负责人证件类型",
    `actual_leg_id_no` varchar(255) NULL COMMENT "实际负责人证件号码",
    `firm_category` varchar(64) NULL COMMENT "企业/个体户 ",
    `reg_address` varchar(64) NULL COMMENT "注册地址",
    `ope_address` varchar(255) NULL COMMENT "经营地址",
    `update_time` bigint NULL COMMENT "更新时间",
    `created_time` bigint NULL COMMENT "创建时间",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `source` varchar(64) NULL COMMENT "数据来源",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-重大税收违法案件"'''

    dwd_q_db_zlr_xiaowei_t_mining_tags_scale = '''CREATE TABLE if not exists %s.%s (
    `firm_eid` varchar(64) NOT NULL COMMENT "企业eid",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "企业社会信用代码",
    `class_code` varchar(64) NULL COMMENT "大类代码",
    `class_name` varchar(64) NULL COMMENT "大类名称",
    `type_id` varchar(64) NULL COMMENT "小类ID",
    `type_name` varchar(64) NULL COMMENT "小类名称",
    `tag_id` varchar(64) NULL COMMENT "标签ID",
    `tag_name` varchar(64) NULL COMMENT "标签名称",
    `unique_id` varchar(64) NULL COMMENT "唯一id",
    `id` bigint NULL COMMENT "加密唯一id",
    `u_tags` int NULL COMMENT "是否隐藏",
    `is_history` int NULL COMMENT "是否历史",
    `create_time` bigint NULL COMMENT "创建时间",
    `row_update_time` datetime NULL COMMENT "数据记录更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    PRIMARY KEY (`firm_eid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-企业规模标签"'''

    dwd_operational_risk_abnormal_tax_payment_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `legal_rep_name` varchar(255) NULL COMMENT "法定代表人姓名",
    `legal_rep_cert_type` varchar(64) NULL COMMENT "法定代表人证件类型",
    `legal_rep_cert_no` varchar(64) NULL COMMENT "法定代表人证件号码",
    `addr` varchar(512) NULL COMMENT "生产经营地址",
    `tax_admin_agency` varchar(64) NULL COMMENT "主管税务机关",
    `taxpayer_no` varchar(64) NULL COMMENT "纳税人识别号",
    `status` varchar(64) NULL COMMENT "纳税人状态",
    `affirm_reason` varchar(255) NULL COMMENT "认定原因",
    `affirm_date` date NULL COMMENT "认定日期",
    `pub_date` date NULL COMMENT "公告日期",
    `affirm_agen` varchar(255) NULL COMMENT "认定单位",
    `judge_agency_addr` varchar(64) NULL COMMENT "认定单位的地址",
    `telephone` varchar(64) NULL COMMENT "认定单位的电话",
    `tax_type` varchar(64) NULL COMMENT "欠税税务种类",
    `tax_owe_amt` varchar(64) NULL COMMENT "欠税金额",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `update_time` bigint NULL COMMENT "更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-纳税非正常户信息"'''

    dwd_operational_risk_land_mortgage = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `land_attributes` varchar(64) NULL COMMENT "土地权属性质与使用权类型",
    `user_code` varchar(64) NULL COMMENT "土地他项权利人证号",
    `land_usecode` varchar(64) NULL COMMENT "土地使用权证号",
    `land_mark` varchar(64) NULL COMMENT "宗地标识",
    `land_code` varchar(255) NULL COMMENT "宗地编号",
    `land_use` varchar(64) NULL COMMENT "抵押土地用途",
    `land_area` decimal(18, 5) NULL COMMENT "土地面积（公顷）",
    `mort_area` decimal(18, 5) NULL COMMENT "抵押面积（公顷）",
    `appraisal_amount` decimal(18, 5) NULL COMMENT "评估金额（万元）",
    `mort_amount` decimal(18, 5) NULL COMMENT "抵押金额（万元）",
    `mort_date` date NULL COMMENT "抵押时间",
    `land_mort_start_date` date NULL COMMENT "土地抵押登记起始日期",
    `land_mort_end_date` date NULL COMMENT "土地抵押结束日期",
    `location` varchar(255) NULL COMMENT "土地位置",
    `province` varchar(64) NULL COMMENT "所在行政区",
    `pledger_name` varchar(255) NULL COMMENT "土地抵押人名称",
    `pledgor_id_type` varchar(64) NULL COMMENT "土地抵押人证件类型",
    `pledgor_id_no` varchar(64) NULL COMMENT "土地抵押人证件号码",
    `pledgor_id` varchar(255) NULL COMMENT "土地抵押人id",
    `pledger_type` varchar(64) NULL COMMENT "土地抵押人类型",
    `mort_name` varchar(255) NULL COMMENT "土地抵押权人名称",
    `mort_id_type` varchar(64) NULL COMMENT "土地抵押权人证件类型",
    `mort_id_no` varchar(64) NULL COMMENT "土地抵押权人证件号码",
    `mort_id` varchar(255) NULL COMMENT "土地抵押权人id",
    `role` varchar(64) NULL COMMENT "角色",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `md5_index` varchar(64) NULL COMMENT "爬虫的唯一值",
    `created_time` bigint NULL COMMENT "创建时间",
    `last_updated_time` bigint NULL COMMENT "最后更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营风险-土地抵押"'''

    dwd_business_info_plots_publicity = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `receiver_name` varchar(512) NULL COMMENT "受让单位名称",
    `receiver_id` varchar(64) NULL COMMENT "受让单位id",
    `land_code` varchar(255) NULL COMMENT "宗地编号",
    `land_use` varchar(64) NULL COMMENT "土地用途",
    `land_area` decimal(18, 5) NULL COMMENT "土地面积（公顷）",
    `use_year` varchar(64) NULL COMMENT "土地使用年限",
    `deal_price` decimal(18, 5) NULL COMMENT "成交价格（万元）",
    `province` varchar(64) NULL COMMENT "所在行政区",
    `location` varchar(512) NULL COMMENT "地块位置",
    `use_situation` varchar(64) NULL COMMENT "土地利用状况",
    `project_name` varchar(255) NULL COMMENT "项目名称",
    `title` varchar(255) NULL COMMENT "标题",
    `release_agency` varchar(64) NULL COMMENT "发布机关（批准单位）",
    `release_date` date NULL COMMENT "发布日期",
    `pub_start_date` date NULL COMMENT "公示开始",
    `pub_end_date` date NULL COMMENT "公示结束",
    `contact_person` varchar(64) NULL COMMENT "联系人",
    `contact_tel` varchar(64) NULL COMMENT "联系电话",
    `zip` varchar(64) NULL COMMENT "邮政编码",
    `addr` varchar(255) NULL COMMENT "单位地址",
    `note` varchar(512) NULL COMMENT "备注",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `created_time` bigint NULL COMMENT "创建时间",
    `last_updated_time` bigint NULL COMMENT "最后更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-地块公示"'''

    dwd_business_info_big_plots_transfer = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `land_use_rights_name` varchar(64) NULL COMMENT "土地使用权人名称",
    `land_use_rights_id` varchar(64) NULL COMMENT "土地使用权人id",
    `parent_company_name` varchar(64) NULL COMMENT "上级公司名称",
    `parent_company_id` varchar(64) NULL COMMENT "上级公司id",
    `role` varchar(64) NULL COMMENT "角色",
    `elect_no` varchar(64) NULL COMMENT "电子监管号",
    `project_name` varchar(64) NULL COMMENT "项目名称",
    `location` varchar(255) NULL COMMENT "土地位置",
    `province` varchar(64) NULL COMMENT "所在行政区",
    `land_area` decimal(18, 5) NULL COMMENT "面积（公顷）",
    `deal_price` decimal(18, 5) NULL COMMENT "成交价格（万元）",
    `use_year` varchar(64) NULL COMMENT "土地使用年限",
    `land_source` varchar(64) NULL COMMENT "土地来源",
    `land_use` varchar(64) NULL COMMENT "土地用途",
    `signing_mode` varchar(64) NULL COMMENT "供地方式",
    `land_level` varchar(64) NULL COMMENT "土地级别",
    `min_floor_area_ratio` decimal(18, 5) NULL COMMENT "约定容积率下限",
    `max_floor_area_ratio` decimal(18, 5) NULL COMMENT "约定容积率上限",
    `signing_date` date NULL COMMENT "合同签订日期",
    `appoint_time` date NULL COMMENT "约定交地日期",
    `appoint_start_time` date NULL COMMENT "约定开工日期",
    `appoint_comp_time` date NULL COMMENT "约定竣工日期",
    `actual_start_time` date NULL COMMENT "实际开工日期",
    `actual_comp_time` date NULL COMMENT "实际竣工日期",
    `approver` varchar(64) NULL COMMENT "批准单位",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `created_time` bigint NULL COMMENT "创建时间",
    `last_updated_time` bigint NULL COMMENT "最后更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-房地产大地块出让"'''

    dwd_business_info_land_acquisition_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `land_use_rights_name` varchar(64) NULL COMMENT "土地使用权人名称",
    `land_use_rights_id` varchar(64) NULL COMMENT "土地使用权人id",
    `parent_company_name` varchar(64) NULL COMMENT "上级公司名称",
    `parent_company_id` varchar(64) NULL COMMENT "上级公司id",
    `role` varchar(64) NULL COMMENT "角色",
    `elect_no` varchar(64) NULL COMMENT "电子监管号",
    `project_name` varchar(64) NULL COMMENT "项目名称",
    `location` varchar(255) NULL COMMENT "土地位置",
    `province` varchar(64) NULL COMMENT "所在行政区",
    `land_area` decimal(18, 5) NULL COMMENT "面积（公顷）",
    `deal_price` decimal(18, 5) NULL COMMENT "成交价格（万元）",
    `use_year` varchar(64) NULL COMMENT "土地使用年限",
    `land_source` varchar(64) NULL COMMENT "土地来源",
    `land_use` varchar(64) NULL COMMENT "土地用途",
    `signing_mode` varchar(64) NULL COMMENT "供地方式",
    `land_level` varchar(64) NULL COMMENT "土地级别",
    `min_floor_area_ratio` decimal(18, 5) NULL COMMENT "约定容积率下限",
    `max_floor_area_ratio` decimal(18, 5) NULL COMMENT "约定容积率上限",
    `signing_date` date NULL COMMENT "合同签订日期",
    `appoint_time` date NULL COMMENT "约定交地日期",
    `appoint_start_time` date NULL COMMENT "约定开工日期",
    `appoint_comp_time` date NULL COMMENT "约定竣工日期",
    `actual_start_time` date NULL COMMENT "实际开工日期",
    `actual_comp_time` date NULL COMMENT "实际竣工日期",
    `approver` varchar(64) NULL COMMENT "批准单位",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `created_time` bigint NULL COMMENT "创建时间",
    `last_updated_time` bigint NULL COMMENT "最后更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-房地产大企业购地"'''

    dwd_business_info_import_export_credit = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `customs_reg_code` varchar(64) NULL COMMENT "海关注册编码",
    `reg_customs` varchar(64) NULL COMMENT "注册海关",
    `reg_date` date NULL COMMENT "注册日期",
    `first_record_date` date NULL COMMENT "最早备案日期",
    `latest_record_date` date NULL COMMENT "最新备案日期",
    `validity_date` date NULL COMMENT "报关有效期",
    `business_category` varchar(64) NULL COMMENT "经营类别",
    `admin_region` varchar(64) NULL COMMENT "行政地区",
    `economic_region` varchar(64) NULL COMMENT "经济地区",
    `special_area` varchar(64) NULL COMMENT "特殊贸易区域",
    `cancel_flag` varchar(64) NULL COMMENT "海关注销标志",
    `annual_report` varchar(64) NULL COMMENT "年报情况",
    `industry` varchar(64) NULL COMMENT "行业",
    `credit_level` varchar(64) NULL COMMENT "信用等级",
    `credit_time` date NULL COMMENT "认定时间",
    `lic_num` varchar(64) NULL COMMENT "认证证书编码",
    `customs_level` varchar(64) NULL COMMENT "海关评级",
    `customs_sanction` longtext NULL COMMENT "海关行政处罚",
    `deci_docu_no` varchar(64) NULL COMMENT "处罚决定文书号",
    `penal_deci_date` date NULL COMMENT "处罚日期",
    `case_type` varchar(64) NULL COMMENT "案件性质",
    `case_litigant` varchar(64) NULL COMMENT "案件当事人",
    `import_export_firm_code` varchar(64) NULL COMMENT "进出口企业代码",
    `ecommerce_type` varchar(64) NULL COMMENT "跨境贸易电子商务类型",
    `record_addr` varchar(64) NULL COMMENT "海关报备地址",
    `record_addr_enname` varchar(512) NULL COMMENT "海关报备地址英文名",
    `abnormal_info` varchar(64) NULL COMMENT "异常信息",
    `reg_addr` varchar(255) NULL COMMENT "工商注册地址",
    `create_time` bigint NULL COMMENT "入库时间",
    `last_updated_time` bigint NULL COMMENT "最后更新时间",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-海关登记信息"'''

    dwd_business_info_taxpayer_reg_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `taxpayer_name` varchar(255) NULL COMMENT "纳税人名称",
    `taxpayer_no` varchar(64) NULL COMMENT "纳税人识别号",
    `qualification_type` varchar(64) NULL COMMENT "纳税人资格类型",
    `affirm_date` date NULL COMMENT "认定日期",
    `validity_date_start` date NULL COMMENT "有效日期起",
    `validity_date_end` date NULL COMMENT "有效日期止",
    `tax_admin_agency` varchar(64) NULL COMMENT "主管税务机关",
    `general_taxpayer_flag` varchar(64) NULL COMMENT "一般纳税人标志位",
    `is_general_taxpayer` varchar(64) NULL COMMENT "是否具有一般纳税人资格",
    `duty` varchar(64) NULL COMMENT "扣缴义务",
    `is_quarterly` varchar(64) NULL COMMENT "是否按季申报",
    `filing_status` varchar(64) NULL COMMENT "出口备案状态",
    `credit_rating` varchar(64) NULL COMMENT "信用评级",
    `credit_rating_date` date NULL COMMENT "信用评级时间",
    `credit_rating_score` decimal(18, 5) NULL COMMENT "信用评级分数",
    `acc_system_name` varchar(64) NULL COMMENT "所属会计制度名称",
    `tax_people` varchar(64) NULL COMMENT "办税人姓名",
    `tax_people_id_type` varchar(64) NULL COMMENT "办税人身份证件类型",
    `tax_people_id_no` varchar(64) NULL COMMENT "办税人身份证件号码",
    `tax_people_telephone` varchar(64) NULL COMMENT "办税人固定电话",
    `tax_people_m_phone` varchar(64) NULL COMMENT "办税人移动电话",
    `fm_name` varchar(64) NULL COMMENT "财务负责人姓名",
    `fm_id_type` varchar(64) NULL COMMENT "财务负责人身份证件类型",
    `fm_id_no` varchar(64) NULL COMMENT "财务负责人身份证件号码",
    `fm_telephone` varchar(64) NULL COMMENT "财务负责人固定电话",
    `fm_m_phone` varchar(64) NULL COMMENT "财务负责人移动电话",
    `tax_agent_name` varchar(64) NULL COMMENT "税务代理人姓名",
    `tax_agent_m_phone` varchar(64) NULL COMMENT "税务代理人移动电话",
    `reg_type` varchar(64) NULL COMMENT "登记注册类型",
    `taxpayer_status` varchar(64) NULL COMMENT "纳税人状态",
    `created_time` varchar(64) NULL COMMENT "创建时间",
    `last_updated_time` varchar(64) NULL COMMENT "最后更新时间",
    `url` varchar(1000) NULL COMMENT "数据源链接",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-纳税人登记信息"'''

    dwd_business_info_tax_credit_rate = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `taxpayer_name` varchar(64) NULL COMMENT "纳税人名称",
    `taxpayer_no` varchar(64) NULL COMMENT "纳税人识别号",
    `tax_credit_rating` varchar(64) NULL COMMENT "纳税信用等级",
    `eval_year` varchar(64) NULL COMMENT "评价年度",
    `eval_department` varchar(64) NULL COMMENT "评价单位",
    `eval_score` decimal(18, 5) NULL COMMENT "评价得分",
    `eval_date` varchar(64) NULL COMMENT "评价时间",
    `created_time` varchar(64) NULL COMMENT "创建时间",
    `last_updated_time` varchar(64) NULL COMMENT "最后更新时间",
    `source` varchar(64) NULL COMMENT "数据来源",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-纳税信用等级"'''

    dwd_business_info_pollution_discharge_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_type_code` varchar(64) NULL COMMENT "证书类型代码",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `content` varchar(64) NULL COMMENT "许可内容",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status` varchar(64) NULL COMMENT "状态",
    `status_code` int NULL COMMENT "状态编码",
    `reason` varchar(64) NULL COMMENT "注销/撤销原因",
    `cancel_date` date NULL COMMENT "注销/撤销日期",
    `province` varchar(64) NULL COMMENT "省份",
    `province_code` int NULL COMMENT "省份代码",
    `addr` varchar(255) NULL COMMENT "地址",
    `area` varchar(64) NULL COMMENT "所在地区",
    `ind_econ_name` varchar(64) NULL COMMENT "行业类别",
    `business_process` text NULL COMMENT "证书业务流程",
    `main_pollutants` int NULL COMMENT "主要污染物类别",
    `atmosphere_pollutants_kind` varchar(512) NULL COMMENT "大气主要污染物种类",
    `atmosphere_pollutants_law` int NULL COMMENT "大气污染物排放规律",
    `atmosphere_pollutants_standard` varchar(1000) NULL COMMENT "大气污染物排放执行标准",
    `water_pollutants_kind` varchar(512) NULL COMMENT "废水主要污染物种类",
    `water_pollutants_law` varchar(512) NULL COMMENT "废水污染物排放规律",
    `water_pollutants_standard` varchar(512) NULL COMMENT "废水污染物排放执行标准",
    `use_right` varchar(512) NULL COMMENT "排污权使用和交易信息",
    `report` text NULL COMMENT "执行报告",
    `supervise_info` varchar(64) NULL COMMENT "监督执法信息",
    `self_monitor_info` varchar(64) NULL COMMENT "自行监测信息",
    `pic1_url_ori` varchar(255) NULL COMMENT "排污许可证正本链接(原始)",
    `pic1_url_oss` varchar(255) NULL COMMENT "排污许可证正本链接(oss)",
    `pic2_url_ori` varchar(255) NULL COMMENT "排污许可证副本链接(原始)",
    `pic2_url_oss` longtext NULL COMMENT "排污许可证副本链接(oss)",
    `related_companies` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-排污许可"'''

    dwd_business_info_plots_announce = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `project_name` varchar(255) NULL COMMENT "项目名称",
    `elect_no` varchar(64) NULL COMMENT "电子监管号",
    `province` varchar(64) NULL COMMENT "所在行政区",
    `location` varchar(255) NULL COMMENT "地块位置",
    `land_use` varchar(64) NULL COMMENT "土地用途",
    `land_area` decimal(18, 5) NULL COMMENT "土地面积（公顷）",
    `deal_price` decimal(18, 5) NULL COMMENT "成交价格（万元）",
    `signing_mode` varchar(64) NULL COMMENT "供地方式",
    `use_year` varchar(64) NULL COMMENT "土地使用年限",
    `ind_econ_name` varchar(64) NULL COMMENT "所属行业名称",
    `land_level` varchar(64) NULL COMMENT "土地级别",
    `land_source` varchar(64) NULL COMMENT "土地来源",
    `min_floor_area_ratio` decimal(18, 5) NULL COMMENT "约定容积率下限",
    `max_floor_area_ratio` decimal(18, 5) NULL COMMENT "约定容积率上限",
    `signing_date` date NULL COMMENT "合同签订日期",
    `appoint_time` date NULL COMMENT "约定交地日期",
    `appoint_start_time` date NULL COMMENT "约定开工日期",
    `appoint_comp_time` date NULL COMMENT "约定竣工日期",
    `actual_start_time` date NULL COMMENT "实际开工日期",
    `actual_comp_time` date NULL COMMENT "实际竣工日期",
    `installment_appoint` text NULL COMMENT "分期支付约定",
    `installment_no` varchar(64) NULL COMMENT "支付期号",
    `appoint_pay_date` date NULL COMMENT "约定支付日期",
    `appoint_pay_amount` decimal(18, 5) NULL COMMENT "约定支付金额",
    `approver` varchar(64) NULL COMMENT "批准单位",
    `release_date` date NULL COMMENT "发布日期",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `created_time` bigint NULL COMMENT "创建时间",
    `last_updated_time` bigint NULL COMMENT "最后更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-土地出让结果公告"'''

    dwd_business_info_land_transfer = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `transferor_name` varchar(255) NULL COMMENT "土地转让方名称",
    `transferor_id` varchar(255) NULL COMMENT "土地转让方id",
    `receiver_name` varchar(255) NULL COMMENT "土地受让方名称",
    `receiver_id` varchar(255) NULL COMMENT "土地受让方id",
    `land_attributes` varchar(64) NULL COMMENT "土地权属性质与使用权类型",
    `land_use` varchar(64) NULL COMMENT "土地用途",
    `land_mark` varchar(64) NULL COMMENT "宗地标识",
    `land_code` varchar(64) NULL COMMENT "宗地编号",
    `land_area` decimal(18, 5) NULL COMMENT "土地面积（公顷）",
    `transfer_type` varchar(64) NULL COMMENT "转让方式",
    `transfer_price` decimal(18, 5) NULL COMMENT "转让价格（万元）",
    `transaction_date` date NULL COMMENT "成交时间",
    `use_year` varchar(64) NULL COMMENT "土地使用年限",
    `use_situation` varchar(64) NULL COMMENT "土地利用状况",
    `land_level` varchar(64) NULL COMMENT "土地级别",
    `location` varchar(255) NULL COMMENT "宗地地址",
    `province` varchar(64) NULL COMMENT "所在行政区",
    `role` varchar(64) NULL COMMENT "角色",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `created_time` bigint NULL COMMENT "创建时间",
    `last_updated_time` bigint NULL COMMENT "最后更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-土地转让"'''

    dwd_business_info_recruitment_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `title` varchar(512) NULL COMMENT "招聘职位",
    `job_type` varchar(64) NULL COMMENT "招聘类型",
    `description` text NULL COMMENT "职位描述",
    `number` varchar(64) NULL COMMENT "招聘人数",
    `experience` varchar(64) NULL COMMENT "经验要求",
    `education` varchar(64) NULL COMMENT "学历要求",
    `skill` varchar(255) NULL COMMENT "技能要求",
    `qualification` varchar(64) NULL COMMENT "资质要求",
    `age` varchar(64) NULL COMMENT "年龄要求",
    `sex` varchar(64) NULL COMMENT "性别要求",
    `salary` varchar(64) NULL COMMENT "月薪",
    `welfare` varchar(255) NULL COMMENT "福利",
    `location` varchar(512) NULL COMMENT "工作地点",
    `city_code` varchar(64) NULL COMMENT "所在城市代码",
    `release_date` date NULL COMMENT "发布日期",
    `start_date` date NULL COMMENT "招聘开始时间",
    `end_date` date NULL COMMENT "招聘结束时间",
    `firm_scale` varchar(64) NULL COMMENT "企业规模",
    `firm_type` varchar(64) NULL COMMENT "企业类别",
    `industry` varchar(512) NULL COMMENT "行业类别",
    `status` varchar(64) NULL COMMENT "公司经营状态",
    `company_addr` varchar(64) NULL COMMENT "公司地址(经营地址)",
    `company_lng` varchar(64) NULL COMMENT "公司经度",
    `company_lat` varchar(64) NULL COMMENT "公司纬度",
    `source` varchar(64) NULL COMMENT "招聘信息来源",
    `source_code` varchar(64) NULL COMMENT "招聘信息来源代码",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `url_app` varchar(255) NULL COMMENT "移动端链接",
    `job_1st_class` varchar(512) NULL COMMENT "一级分类",
    `job_2nd_class` varchar(255) NULL COMMENT "二级分类",
    `job_3rd_class` varchar(255) NULL COMMENT "三级分类",
    `job_4th_class` varchar(64) NULL COMMENT "四级分类",
    `short_name` varchar(512) NULL COMMENT "公司简称",
    `flag` int NULL COMMENT "标志位",
    `label` varchar(1000) NULL COMMENT "标签",
    `created_time` bigint NULL COMMENT "创建时间",
    `last_updated_time` bigint NULL COMMENT "最后更新时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-招聘信息"'''

    dwd_intellectual_property_firm_qualification = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(255) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status` varchar(64) NULL COMMENT "状态",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province` varchar(64) NULL COMMENT "省份",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(1000) NULL COMMENT "数据源链接",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    `table_en_name` varchar(64) NULL COMMENT "分表英文名称",
    `table_cn_name` varchar(64) NULL COMMENT "分表中文名称",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "知识产权-企业资质"'''

    dwd_intellectual_property_website_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(1000) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `web_record_no` varchar(255) NULL COMMENT "网站备案编号",
    `web_addr` varchar(512) NULL COMMENT "网站（网店）地址",
    `web_domain` varchar(512) NULL COMMENT "网站（网店）域名",
    `domain_id` varchar(64) NULL COMMENT "网站（网店）域名id",
    `web_name` varchar(512) NULL COMMENT "网站（网店）名称",
    `web_id` varchar(64) NULL COMMENT "网站id",
    `web_type` varchar(64) NULL COMMENT "网站（网店）类型",
    `web_source` varchar(64) NULL COMMENT "网址来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `review_date` date NULL COMMENT "审核日期",
    `record_agency` varchar(64) NULL COMMENT "备案地公安机关",
    `is_harmful_info` int NULL COMMENT "是否包含有害信息",
    `principal` varchar(64) NULL COMMENT "负责人",
    `source` varchar(64) NULL COMMENT "数据来源",
    `source_updated_time` varchar(64) NULL COMMENT "数据源更新时间",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "知识产权-网站信息"'''

    dwd_intellectual_property_intellectual_property_pledge = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `product_name` varchar(255) NULL COMMENT "产品名称",
    `product_type` varchar(64) NULL COMMENT "产品种类",
    `src_product_type` varchar(255) NULL COMMENT "产品种类(原始值)",
    `product_reg_no` varchar(255) NULL COMMENT "产品注册号",
    `pledgor_name` varchar(255) NULL COMMENT "出质人名称",
    `pledgee_name` varchar(64) NULL COMMENT "质权人名称",
    `pledge_reg_period` varchar(64) NULL COMMENT "质权登记期限",
    `pledge_start_date` date NULL COMMENT "质权登记日期起",
    `pledge_end_date` date NULL COMMENT "质权登记日期止",
    `public_date` date NULL COMMENT "公示日期",
    `status` varchar(64) NULL COMMENT "状态",
    `pledgor_eid` varchar(64) NULL COMMENT "出质人（公司）eid",
    `pledgor_type` varchar(64) NULL COMMENT "出质人（公司）类型",
    `pledgee_eid` varchar(64) NULL COMMENT "质权人（公司）eid",
    `pledgee_type` varchar(64) NULL COMMENT "质权人（公司）类型",
	`local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "知识产权-知识产权出质"'''

    dwd_business_info_biding_related = '''CREATE TABLE if not exists %s.%s (
    `firm_eid` varchar(64) NOT NULL COMMENT "企业eid",
    `id` varchar(64) NOT NULL COMMENT "关联u_id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `role` int NULL COMMENT "角色",
    `title` varchar(512) NULL COMMENT "公告标题",
    `bid_win_amount` decimal(30, 6) NULL COMMENT "中标金额(万元)",
    `total_bid_win_amount` decimal(30, 6) NULL COMMENT "项目总中标金额(万元)",
    `notice_type_main` int NULL COMMENT "公告类型大类",
    `notice_type_sub` int NULL COMMENT "公告类型小类",
    `industry_code` int NULL COMMENT "招投标行业代码",
    `publish_time` datetime NULL COMMENT "发布时间",
    `area_code` varchar(64) NULL COMMENT "区域码",
    `province_name` varchar(64) NULL COMMENT "省-名称",
    `city_name` varchar(64) NULL COMMENT "市-名称",
    `county_name` varchar(64) NULL COMMENT "区/县-名称",
    `proprietor_company` longtext NULL COMMENT "招标单位",
    `agency_company` text NULL COMMENT "代理机构",
    `winner_company` longtext NULL COMMENT "中标单位",
    `winner_candidate` longtext NULL COMMENT "中标候选人",
    `related_construction` longtext NULL COMMENT "其他相关单位",
    `is_history` int NULL COMMENT "是否历史",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`firm_eid`,`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-招投标关系表"'''

    dwd_business_info_biding_basic_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "加密唯一id",
    `title` varchar(512) NULL COMMENT "公告标题",
    `notice_type_main` int NULL COMMENT "公告类型大类",
    `notice_type_sub` int NULL COMMENT "公告类型小类",
    `industry_code` int NULL COMMENT "招投标行业代码",
    `project_name` varchar(512) NULL COMMENT "项目名称",
    `project_number` varchar(512) NULL COMMENT "项目编号",
    `content` longtext NULL COMMENT "正文",
    `content_html` longtext NULL COMMENT "正文(html格式)",
    `publish_time` datetime NULL COMMENT "发布时间",
    `area_code` varchar(64) NULL COMMENT "区域码",
    `province_name` varchar(64) NULL COMMENT "省",
    `city_name` varchar(64) NULL COMMENT "市",
    `county_name` varchar(64) NULL COMMENT "区/县",
    `proprietor_company` longtext NULL COMMENT "招标单位",
    `agency_company` text NULL COMMENT "代理机构",
    `winner_company` longtext NULL COMMENT "中标单位",
    `winner_candidate` longtext NULL COMMENT "中标候选人",
    `related_construction` longtext NULL COMMENT "其他相关单位",
    `bid_win_amount` decimal(30, 6) NULL COMMENT "中标金额(万元)",
    `project_budget_money` varchar(64) NULL COMMENT "预算控制价",
    `project_time_limit` varchar(64) NULL COMMENT "工期",
    `entry_start_time` datetime NULL COMMENT "报名开始日期",
    `entry_end_time` datetime NULL COMMENT "报名结束日期",
    `bid_open_time` datetime NULL COMMENT "开标时间",
    `project_fund_source` varchar(255) NULL COMMENT "资金来源",
    `url` text NULL COMMENT "公告来源地址",
    `is_history` int NULL COMMENT "是否历史",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "经营信息-招投标基本信息表"'''

    dwd_business_info_building_qualification = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(255) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `cert_name` text NULL COMMENT "资质名称",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(512) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `legal_rep` varchar(64) NULL COMMENT "法人",
    `firm_type` varchar(64) NULL COMMENT "企业类型",
    `ope_address` varchar(255) NULL COMMENT "经营地址",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-建筑类资质"'''

    dwd_business_info_ccc_product_certification = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(1000) NULL COMMENT "数据源链接",
    `firm_en_name` varchar(64) NULL COMMENT "企业英文名",
    `info_report_date` date NULL COMMENT "信息上报日期",
    `product_type` varchar(255) NULL COMMENT "产品类别",
    `certification_standard` varchar(512) NULL COMMENT "认证标准和技术要求",
    `product_name` varchar(255) NULL COMMENT "产品名称",
    `product_en_name` varchar(255) NULL COMMENT "产品英文名",
    `product_unit_main` text NULL COMMENT "产品名称及单元（主）",
    `product_unit_se` varchar(512) NULL COMMENT "产品名称及单元（次）",
    `product_model` text NULL COMMENT "规格型号",
    `annex_download_url` text NULL COMMENT "证书附件下载连接",
    `principal_name` varchar(255) NULL COMMENT "认证委托人企业名称",
    `principal_eid` varchar(64) NULL COMMENT "认证委托人企业id",
    `principal_area` varchar(64) NULL COMMENT "认证委托人所在国别地区",
    `principal_code` varchar(64) NULL COMMENT "认证委托人统一社会信用代码/组织机构代码",
    `principal_address` varchar(255) NULL COMMENT "认证委托人地址",
    `producer_name` varchar(255) NULL COMMENT "生产者企业名称",
    `producer_eid` varchar(64) NULL COMMENT "生产者企业eid",
    `producer_area` varchar(64) NULL COMMENT "生产者所在国别地区",
    `producer_code` varchar(64) NULL COMMENT "生产者统一社会信用代码/组织机构代码",
    `producer_address` varchar(255) NULL COMMENT "生产者地址",
    `applicant_name` varchar(64) NULL COMMENT "申请人中文名",
    `applicant_en_name` varchar(255) NULL COMMENT "申请人英文名",
    `manufacturer_name` varchar(64) NULL COMMENT "制造商中文名",
    `manufacturer_en_name` varchar(255) NULL COMMENT "制造商英文名",
    `manufacturer_address` varchar(64) NULL COMMENT "制造商地址",
    `manufacturer_eid` varchar(64) NULL COMMENT "制造商eid",
    `cer_agency` varchar(64) NULL COMMENT "发证机构名称",
    `agency_eid` varchar(64) NULL COMMENT "发证机构eid",
    `agency_no` varchar(64) NULL COMMENT "发证机构批准号",
    `agency_date` date NULL COMMENT "发证机构有效期",
    `agency_phone` varchar(64) NULL COMMENT "发证机构电话",
    `agency_domain` varchar(64) NULL COMMENT "发证机构网址",
    `agency_status` varchar(64) NULL COMMENT "发证机构状态",
    `agency_address` varchar(64) NULL COMMENT "发证机构地址",
    `agency_scope` text NULL COMMENT "发证结构业务范围",
    `agency_punishment_info` varchar(64) NULL COMMENT "处罚信息",
    `pause_start_date` date NULL COMMENT "暂停开始日期",
    `pause_end_date` date NULL COMMENT "暂停结束日期",
    `change_date` date NULL COMMENT "变更日期",
    `rev_date` date NULL COMMENT "撤销日期",
    `cancel_date` date NULL COMMENT "注销日期",
    `first_date` date NULL COMMENT "初次获证日期",
    `cert_change_date` date NULL COMMENT "证书状态变化时间",
    `cert_change_reason` varchar(255) NULL COMMENT "证书状态变化原因",
    `self_declaration_no` varchar(64) NULL COMMENT "转为自我声明后的编号",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-CCC产品认证"'''

    dwd_business_info_ccc_self_declaration = '''CREATE TABLE if not exists %s.%s (
    `self_declaration_no` varchar(64) NOT NULL COMMENT "自我声明编号",
    `self_declaration_state` varchar(64) NULL COMMENT "自我声明状态",
    `self_declaration_date` date NULL COMMENT "自我声明时间",
    `end_date` date NULL COMMENT "到期日期",
    `product_type` varchar(255) NULL COMMENT "产品类别",
    `rules` varchar(64) NULL COMMENT "实施规则",
    `declaration` text NULL COMMENT "强制性认证产品符合性声明",
    `license_no` varchar(64) NULL COMMENT "CCC证书编号",
    `producer` varchar(255) NULL COMMENT "生产者",
    `producer_eid` varchar(64) NULL COMMENT "生产者eid",
    `producer_area` varchar(64) NULL COMMENT "生产者地区",
    `producer_code` varchar(64) NULL COMMENT "生产者统一社会信用代码",
    `producer_address` varchar(255) NULL COMMENT "生产者地址",
    `producer_person` varchar(64) NULL COMMENT "生产者联系人/电话",
    `producer_phone` varchar(64) NULL COMMENT "生产者固定电话",
    `producer_email` varchar(64) NULL COMMENT "生产者电子邮箱",
    `representative_name` varchar(64) NULL COMMENT "授权代表名称",
    `representative_eid` varchar(64) NULL COMMENT "授权代表id",
    `representative_area` varchar(64) NULL COMMENT "授权代表地区",
    `representative_code` varchar(64) NULL COMMENT "授权代表统一社会信用代码",
    `representative_person` varchar(64) NULL COMMENT "授权代表联系人/电话",
    `representative_address` varchar(255) NULL COMMENT "授权代表地址",
    `representative_phone` varchar(64) NULL COMMENT "授权代表固定电话",
    `representative_email` varchar(64) NULL COMMENT "授权代表电子邮箱",
    `laboratory_name` varchar(64) NULL COMMENT "实验室名称",
    `laboratory_eid` varchar(64) NULL COMMENT "实验室id",
    `laboratory_kind` varchar(64) NULL COMMENT "实验室类型",
    `laboratory_code` varchar(64) NULL COMMENT "实验室统一社会信用代码",
    `remark` varchar(64) NULL COMMENT "备注",
    `product_ent_info` text NULL COMMENT "生产企业信息",
    `production_info` text NULL COMMENT "产品信息",
    `source` varchar(64) NULL COMMENT "来源",
    `url` varchar(255) NULL COMMENT "来源网址",
    `is_history` int NULL COMMENT "是否历史",
    `create_time` bigint NULL COMMENT "创建日期",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "本地数据更新时间",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`self_declaration_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-CCC自我声明表"'''

    dwd_business_info_cesi_product_certification = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(255) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `manufacturer` varchar(64) NULL COMMENT "制造商",
    `manufacturer_eid` varchar(64) NULL COMMENT "制造商eid",
    `producer_name` varchar(64) NULL COMMENT "生产厂家",
    `producer_eid` varchar(64) NULL COMMENT "生产厂家eid",
    `product_name` varchar(512) NULL COMMENT "产品名称",
    `product_model` text NULL COMMENT "产品型号",
    `certification_standard` varchar(1000) NULL COMMENT "认证标准",
    `cert_change_date` date NULL COMMENT "证书变化日期",
    `cert_change_reason` varchar(255) NULL COMMENT "原因",
    `remark` varchar(64) NULL COMMENT "备注",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-CESI产品认证"'''

    dwd_business_info_computer_project_manager = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(255) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `name` varchar(64) NULL COMMENT "姓名",
    `level` varchar(64) NULL COMMENT "资质等级",
    `appr_date` date NULL COMMENT "审核日期",
    `location` varchar(64) NULL COMMENT "所在地",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-计算机信息系统集成项目经理资质"'''

    dwd_business_info_cosmetic_produce_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(255) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `address` varchar(255) NULL COMMENT "企业地址",
    `warehouse_address` varchar(64) NULL COMMENT "仓库地址",
    `legal_rep` varchar(64) NULL COMMENT "法定代表人",
    `principal` varchar(64) NULL COMMENT "企业负责人",
    `quality_principal` varchar(64) NULL COMMENT "质量负责人",
    `perm_item` varchar(512) NULL COMMENT "许可项目",
    `signer` varchar(64) NULL COMMENT "签发人",
    `sup_agency` varchar(64) NULL COMMENT "日常监管机构",
    `sup_staff` varchar(64) NULL COMMENT "日常监管人员",
    `telephone` varchar(64) NULL COMMENT "监督举报电话",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-化妆品生产许可信息"'''

    dwd_business_info_drug_business = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `reg_address` varchar(512) NULL COMMENT "注册地址",
    `warehouse_address` varchar(512) NULL COMMENT "仓库地址",
    `legal_rep` varchar(64) NULL COMMENT "法定代表人",
    `principal` varchar(64) NULL COMMENT "企业负责人",
    `quality_principal` varchar(64) NULL COMMENT "质量负责人",
    `ope_type` varchar(64) NULL COMMENT "经营方式",
    `ope_scope` varchar(512) NULL COMMENT "经营范围",
    `database_inquire` varchar(64) NULL COMMENT "相关数据库查询",
    `remark` varchar(512) NULL COMMENT "备注",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-药品经营企业"'''

    dwd_business_info_domestic_drug_approve_no = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(255) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `product_name` varchar(64) NULL COMMENT "产品名称",
    `product_en_name` varchar(255) NULL COMMENT "英文名称",
    `commodity_name` varchar(64) NULL COMMENT "商品名",
    `medicine_type` varchar(64) NULL COMMENT "剂型",
    `product_model` varchar(64) NULL COMMENT "规格",
    `license_holder` varchar(64) NULL COMMENT "上市许可持有人",
    `production_address` varchar(255) NULL COMMENT "生产地址",
    `product_type` varchar(64) NULL COMMENT "产品类型",
    `src_license_no` varchar(64) NULL COMMENT "原批准文号",
    `drug_native_code` varchar(255) NULL COMMENT "药品本位码",
    `drug_native_remark` varchar(512) NULL COMMENT "药品本位码备注",
    `database_inquire` varchar(64) NULL COMMENT "相关数据库查询",
    `remark` varchar(64) NULL COMMENT "备注",
    `attention` varchar(512) NULL COMMENT "注",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-国产药品批准文号"'''

    dwd_business_info_drug_produce_firm = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `type_code` varchar(64) NULL COMMENT "分类码",
    `legal_rep` varchar(64) NULL COMMENT "法定代表人",
    `principal` varchar(64) NULL COMMENT "企业负责人",
    `quality_principal` varchar(64) NULL COMMENT "质量负责人",
    `reg_address` varchar(255) NULL COMMENT "注册地址",
    `production_address` varchar(512) NULL COMMENT "生产地址",
    `production_scope` varchar(512) NULL COMMENT "生产范围",
    `signer` varchar(64) NULL COMMENT "签发人",
    `sup_agency` varchar(255) NULL COMMENT "日常监管机构",
    `sup_staff` varchar(64) NULL COMMENT "日常监管人员",
    `telephone` varchar(64) NULL COMMENT "监督举报电话",
    `database_inquire` varchar(64) NULL COMMENT "相关数据库查询",
    `remark` varchar(255) NULL COMMENT "备注",
    `attention` varchar(255) NULL COMMENT "注",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-药品生产企业"'''

    dwd_business_info_elect_authen_serv_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(255) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-电子认证服务机构许可证"'''

    dwd_business_info_real_estate_develop_firm = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `level` varchar(64) NULL COMMENT "证书等级",
    `address` varchar(255) NULL COMMENT "企业地址",
    `legal_rep` varchar(64) NULL COMMENT "法定代表人",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-房地产开发企业资质"'''

    dwd_business_info_fire_fighting_system = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `product_type` varchar(64) NULL COMMENT "产品类别",
    `product_name` varchar(64) NULL COMMENT "产品名称",
    `product_model` text NULL COMMENT "产品型号",
    `principal_name` varchar(64) NULL COMMENT "认证委托人",
    `principal_used_name` varchar(64) NULL COMMENT "认证委托人曾用名",
    `principal_eid` varchar(64) NULL COMMENT "认证委托人eid",
    `principal_address` varchar(255) NULL COMMENT "认证委托人地址",
    `manufacturer_name` varchar(64) NULL COMMENT "制造商名称",
    `manufacturer_eid` varchar(64) NULL COMMENT "制造商eid",
    `manufacturer_address` varchar(255) NULL COMMENT "制造商地址",
    `check_basis` varchar(64) NULL COMMENT "鉴定依据",
    `check_reason` varchar(255) NULL COMMENT "执行标准/校验依据",
    `zip_code` varchar(64) NULL COMMENT "邮编",
    `contacts` varchar(64) NULL COMMENT "联系人",
    `telephone` varchar(64) NULL COMMENT "电话",
    `production_address` varchar(255) NULL COMMENT "生产地址",
    `report_no` text NULL COMMENT "检验报告编号",
    `report_pub_date` date NULL COMMENT "报告签发日期",
    `check_unit` varchar(64) NULL COMMENT "检验机构",
    `check_type` varchar(64) NULL COMMENT "校验类别",
    `sample_grade` varchar(64) NULL COMMENT "样品等级",
    `check_project` varchar(255) NULL COMMENT "校验项目",
    `check_result` varchar(64) NULL COMMENT "校验结果",
    `authen_model` varchar(64) NULL COMMENT "产品基本认证模式",
    `change_date` date NULL COMMENT "换证日期",
    `check_report` text NULL COMMENT "检验报告",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-灭火系统"'''

    dwd_business_info_food_additive_produce_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(255) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `level` varchar(64) NULL COMMENT "证书等级",
    `address` varchar(64) NULL COMMENT "住所",
    `product_type` varchar(64) NULL COMMENT "产品类别",
    `remark` varchar(64) NULL COMMENT "备注",
    `production_address` varchar(255) NULL COMMENT "生产地址",
    `product_model` text NULL COMMENT "类型",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-食品添加剂生产许可证"'''

    dwd_business_info_food_business_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `legal_rep` varchar(64) NULL COMMENT "法定代表人",
    `address` varchar(512) NULL COMMENT "住所",
    `ope_address` varchar(512) NULL COMMENT "经营场所",
    `identity` varchar(255) NULL COMMENT "主体业态",
    `ope_item` text NULL COMMENT "经营项目",
    `sup_agency` varchar(64) NULL COMMENT "日常监督管理机构",
    `sup_staff` varchar(64) NULL COMMENT "日常监督管理人员",
    `telephone` varchar(64) NULL COMMENT "投诉举报电话",
    `signer` varchar(64) NULL COMMENT "签发人",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-食品经营许可证"'''

    dwd_business_info_food_farming_product_authen = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部内名称",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(1000) NULL COMMENT "数据源链接",
    `first_date` date NULL COMMENT "初次获证日期",
    `info_report_date` date NULL COMMENT "信息上报日期",
    `mark` varchar(64) NULL COMMENT "证书使用的认可标识",
    `authen_scope` varchar(1000) NULL COMMENT "认证范围",
    `authen_staff_num` varchar(64) NULL COMMENT "认证相关员工数量",
    `authen_n_basis` varchar(512) NULL COMMENT "认证依据",
    `cover_address` varchar(255) NULL COMMENT "认证覆盖的场所名称及地址",
    `authen_type` varchar(64) NULL COMMENT "认证类型/认证选项",
    `authen_item` varchar(64) NULL COMMENT "认证项目",
    `cover_business_scope` varchar(255) NULL COMMENT "认证覆盖的业务范围",
    `change_type` varchar(64) NULL COMMENT "变更类别",
    `cover_staff_num` varchar(64) NULL COMMENT "本证书体系覆盖人数",
    `product_name` varchar(64) NULL COMMENT "产品名称",
    `appr_production` varchar(64) NULL COMMENT "批准产量(吨）",
    `is_cover_multi_address` varchar(64) NULL COMMENT "是否覆盖多场所",
    `authen_level` varchar(64) NULL COMMENT "认证级别",
    `gap_reg_no` varchar(64) NULL COMMENT "GAP注册号",
    `authen_model` varchar(64) NULL COMMENT "认证模式",
    `sup_times` varchar(64) NULL COMMENT "监督次数",
    `re_authen_times` varchar(64) NULL COMMENT "再次认证次数",
    `annex_download_url` varchar(64) NULL COMMENT "证书附件下载",
    `location` varchar(255) NULL COMMENT "组织地址",
    `zip_code` varchar(64) NULL COMMENT "邮政编码",
    `producer_name` varchar(64) NULL COMMENT "获证组织/生产企业名称",
    `producer_eid` varchar(64) NULL COMMENT "获证组织/生产企业eid",
    `producer_code` varchar(64) NULL COMMENT "获证组织/生产企业统一社会信用代码",
    `producer_area` varchar(64) NULL COMMENT "获证组织/生产企业所在国别地区",
    `producer_address` varchar(255) NULL COMMENT "获证组织/生产企业地址",
    `change_history` text NULL COMMENT "历史变更轨迹",
    `principal_name` varchar(64) NULL COMMENT "认证委托人名称",
    `principal_zip_code` varchar(64) NULL COMMENT "认证委托人邮政编码",
    `principal_area` varchar(64) NULL COMMENT "认证委托人所在国别地区",
    `principal_code` varchar(64) NULL COMMENT "认证委托人统一社会信用代码/组织机构代码",
    `principal_address` varchar(255) NULL COMMENT "认证委托人地址",
    `authen_field` varchar(64) NULL COMMENT "服务认证所属领域",
    `trademark_name` varchar(64) NULL COMMENT "商标名称",
    `firm_info_code` varchar(64) NULL COMMENT "企业信息码",
    `pause_start_date` date NULL COMMENT "暂停开始日期",
    `pause_end_date` date NULL COMMENT "暂停结束日期",
    `rev_date` date NULL COMMENT "撤销日期",
    `cancel_date` date NULL COMMENT "注销日期",
    `cer_agency` varchar(64) NULL COMMENT "发证机构名称",
    `agency_no` varchar(64) NULL COMMENT "发证机构批准号",
    `agency_date` date NULL COMMENT "发证机构有效期",
    `agency_status` varchar(64) NULL COMMENT "发证机构状态",
    `agency_domain` varchar(64) NULL COMMENT "发证机构网址",
    `agency_address` varchar(255) NULL COMMENT "发证机构地址",
    `agency_scope` text NULL COMMENT "发证机构业务范围",
    `agency_punishment_info` varchar(64) NULL COMMENT "发证机构处罚信息",
    `production_info` text NULL COMMENT "产品信息",
    `is_sub_cert` varchar(64) NULL COMMENT "是否子证书",
    `main_cert_no` varchar(64) NULL COMMENT "主证书证书号",
    `change_date` date NULL COMMENT "变更日期",
    `rev_reason` varchar(512) NULL COMMENT "撤销原因",
    `change_cert_date` date NULL COMMENT "换证日期",
    `src_agency_no` varchar(64) NULL COMMENT "原颁证机构批准号",
    `src_license_no` varchar(64) NULL COMMENT "原认证证书号",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-食品农产品认证"'''

    dwd_business_info_export_food_product_firm = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `reg_address` varchar(255) NULL COMMENT "注册地址",
    `country` text NULL COMMENT "国家地区",
    `product_name` text NULL COMMENT "产品名称",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-出口食品生产企业备案"'''

    dwd_business_info_food_product_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `cert_type` varchar(64) NULL COMMENT "证书类别",
    `legal_rep` varchar(64) NULL COMMENT "法定代表人",
    `address` varchar(255) NULL COMMENT "住所",
    `production_address` varchar(512) NULL COMMENT "生产地址",
    `product_type` varchar(255) NULL COMMENT "食品类别",
    `check_type` varchar(64) NULL COMMENT "检验方式",
    `level` varchar(64) NULL COMMENT "级别",
    `sup_agency` varchar(255) NULL COMMENT "日常监督管理机构",
    `sup_staff` varchar(255) NULL COMMENT "日常监督管理人员",
    `signer` varchar(64) NULL COMMENT "签发人",
    `composition` text NULL COMMENT "许可明细",
    `license_content_details` text NULL COMMENT "许可明细（详）",
    `product_name` varchar(255) NULL COMMENT "产品名称",
    `remark` varchar(255) NULL COMMENT "备注",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-食品生产许可证"'''

    dwd_business_info_ind_product_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `attention` varchar(255) NULL COMMENT "说明",
    `address` varchar(255) NULL COMMENT "住所",
    `production_address` varchar(255) NULL COMMENT "生产地址",
    `product_model` varchar(512) NULL COMMENT "产品规格",
    `composition` text NULL COMMENT "许可明细",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-工业产品生产许可证"'''

    dwd_business_info_voluntary_ind_product_authen = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(1000) NULL COMMENT "数据源链接",
    `first_date` date NULL COMMENT "初次获证日期",
    `info_report_date` date NULL COMMENT "信息上报日期",
    `authen_item` varchar(64) NULL COMMENT "认证项目",
    `mark` varchar(64) NULL COMMENT "产品使用的认可标识",
    `authen_n_basis` text NULL COMMENT "认证依据的标准和技术要求",
    `product_name` text NULL COMMENT "产品名称及单元",
    `product_model` text NULL COMMENT "规格型号",
    `annex_download_url` varchar(255) NULL COMMENT "证书附件下载",
    `principal_name` varchar(255) NULL COMMENT "认证委托人名称",
    `principal_eid` varchar(64) NULL COMMENT "认证委托人id",
    `principal_address` varchar(512) NULL COMMENT "认证委托人地址",
    `producer_name` varchar(255) NULL COMMENT "生产者（制造商）名称",
    `producer_eid` varchar(64) NULL COMMENT "生产者（制造商）id",
    `producer_address` varchar(512) NULL COMMENT "生产者（制造商）地址",
    `produce_firm_name` varchar(512) NULL COMMENT "生产企业基本名称",
    `produce_firm_eid` varchar(64) NULL COMMENT "生产企业eid",
    `produce_firm_address` varchar(512) NULL COMMENT "生产企业基本地址",
    `cer_agency` varchar(64) NULL COMMENT "发证机构名称",
    `agency_eid` varchar(64) NULL COMMENT "发证机构id",
    `agency_no` varchar(64) NULL COMMENT "发证机构批准号",
    `agency_date` date NULL COMMENT "发证机构有效期",
    `agency_status` varchar(64) NULL COMMENT "发证机构状态",
    `agency_domain` varchar(64) NULL COMMENT "发证机构网址",
    `agency_address` varchar(255) NULL COMMENT "发证机构地址",
    `agency_scope` text NULL COMMENT "发证机构业务范围",
    `agency_punishment_info` varchar(64) NULL COMMENT "发证机构处罚信息",
    `pause_start_date` date NULL COMMENT "暂停开始日期",
    `pause_end_date` date NULL COMMENT "暂停结束日期",
    `cancel_date` date NULL COMMENT "注销日期",
    `rev_date` date NULL COMMENT "撤销日期",
    `change_date` date NULL COMMENT "变更日期",
    `sup_times` varchar(64) NULL COMMENT "监督次数",
    `re_authen_times` varchar(64) NULL COMMENT "再次认证次数",
    `cover_business_scope` varchar(64) NULL COMMENT "认证覆盖的业务范围",
    `is_cover_multi_address` varchar(64) NULL COMMENT "是否覆盖多场所",
    `cover_address` varchar(64) NULL COMMENT "认证覆盖的场所名称及地址",
    `voluntary_authen_field` varchar(64) NULL COMMENT "自愿性认证领域",
    `product_type` varchar(64) NULL COMMENT "产品分类",
    `change_type` varchar(64) NULL COMMENT "变更类别",
    `change_cert_date` date NULL COMMENT "换证时间",
    `is_sub_cert` varchar(64) NULL COMMENT "是否是子证书",
    `main_cert_no` varchar(64) NULL COMMENT "主认证证书号",
    `producer_area` varchar(64) NULL COMMENT "获证组织所在国别和地区",
    `producer_area_code` varchar(64) NULL COMMENT "获证组织所在国别和地区代码",
    `cover_staff_num` varchar(64) NULL COMMENT "本证书体系覆盖人数",
    `producer_code` varchar(64) NULL COMMENT "获证组织统一社会信用代码/组织机构代码",
    `org_address` varchar(64) NULL COMMENT "获证组织地址",
    `authen_field` varchar(64) NULL COMMENT "服务认证所属领域",
    `change_history` varchar(1000) NULL COMMENT "历史变更轨迹",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-自愿性工业产品认证"'''

    dwd_business_info_gmp_gsp_authen = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `address` varchar(255) NULL COMMENT "地址",
    `authen_scope` varchar(512) NULL COMMENT "认证范围",
    `continue_date` date NULL COMMENT "批准延续日期",
    `extend_date` date NULL COMMENT "有效期延续至",
    `continue_scope` varchar(255) NULL COMMENT "批准延续认证范围",
    `gmp_version` varchar(64) NULL COMMENT "认证GMP版本",
    `remark` varchar(255) NULL COMMENT "备注",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-GMP-GSP认证"'''

    dwd_business_info_manage_system_authen = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(1000) NULL COMMENT "数据源链接",
    `location` varchar(64) NULL COMMENT "组织地址",
    `zip_code` varchar(64) NULL COMMENT "邮政编码",
    `info_report_date` varchar(64) NULL COMMENT "信息上报日期",
    `mark` varchar(64) NULL COMMENT "证书使用的认可标识",
    `authen_n_basis` varchar(512) NULL COMMENT "认证依据",
    `annex_download_url` text NULL COMMENT "证书附件下载",
    `cer_agency` varchar(64) NULL COMMENT "发证机构名称",
    `agency_eid` varchar(64) NULL COMMENT "发证机构eid",
    `agency_no` varchar(64) NULL COMMENT "发证机构批准号",
    `agency_date` varchar(64) NULL COMMENT "发证机构有效期",
    `agency_phone` varchar(64) NULL COMMENT "发证机构电话",
    `agency_domain` varchar(64) NULL COMMENT "发证机构网址",
    `agency_status` varchar(64) NULL COMMENT "发证机构状态",
    `agency_address` varchar(255) NULL COMMENT "发证机构地址",
    `agency_scope` text NULL COMMENT "发证结构业务范围",
    `agency_punishment_info` varchar(64) NULL COMMENT "发证机构处罚信息",
    `pause_start_date` varchar(64) NULL COMMENT "暂停开始日期",
    `pause_end_date` varchar(64) NULL COMMENT "暂停结束日期",
    `change_date` varchar(64) NULL COMMENT "变更日期",
    `rev_date` varchar(64) NULL COMMENT "撤销日期",
    `cancel_date` varchar(64) NULL COMMENT "注销日期",
    `first_date` varchar(64) NULL COMMENT "初次获证日期",
    `sup_times` varchar(64) NULL COMMENT "监督次数",
    `re_authen_times` varchar(64) NULL COMMENT "再认证次数",
    `authen_item` varchar(64) NULL COMMENT "认证项目",
    `cover_business_scope` text NULL COMMENT "认证覆盖的业务范围",
    `is_cover_multi_address` varchar(64) NULL COMMENT "是否覆盖多场所",
    `cover_address` varchar(512) NULL COMMENT "认证覆盖的场所名称及地址",
    `change_type` varchar(64) NULL COMMENT "变更类别",
    `is_sub_cert` varchar(64) NULL COMMENT "是否是子证书",
    `main_cert_no` varchar(64) NULL COMMENT "主认证证书号",
    `producer_name` varchar(255) NULL COMMENT "获证组织/生产企业名称",
    `producer_eid` varchar(64) NULL COMMENT "获证组织/生产企业eid",
    `producer_area` varchar(64) NULL COMMENT "获证组织所在国别和地区",
    `producer_area_code` varchar(64) NULL COMMENT "获证组织所在国别和地区编码",
    `cover_staff_num` varchar(64) NULL COMMENT "本证书体系覆盖人数",
    `producer_code` varchar(64) NULL COMMENT "获证组织统一社会信用代码/组织机构代码",
    `producer_address` varchar(512) NULL COMMENT "获证组织地址",
    `authen_staff_num` varchar(64) NULL COMMENT "认证相关员工数量",
    `authen_scope` varchar(64) NULL COMMENT "认证范围",
    `production_info` varchar(64) NULL COMMENT "产品信息",
    `change_history` text NULL COMMENT "证书变化历史轨迹",
    `authen_type` varchar(64) NULL COMMENT "认证类型",
    `authen_level` varchar(64) NULL COMMENT "认证级别",
    `authen_model` varchar(64) NULL COMMENT "认证模式",
    `rev_reason` varchar(64) NULL COMMENT "撤销原因",
    `change_cert_date` varchar(64) NULL COMMENT "换证日期",
    `src_agency_no` varchar(64) NULL COMMENT "原颁证机构批准号",
    `src_license_no` varchar(64) NULL COMMENT "原认证证书号",
    `gap_no` varchar(64) NULL COMMENT "GAP注册号",
    `bound` text NULL COMMENT "获证组织能源管理体系边界",
    `cover_scope` varchar(512) NULL COMMENT "EC9000证书",
    `authen_field` varchar(64) NULL COMMENT "服务认证所属领域",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-管理体系认证"'''

    dwd_business_info_medical_device_business_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `kind` varchar(64) NULL COMMENT "资质类别",
    `legal_rep` varchar(64) NULL COMMENT "企业法定代表人",
    `principal` varchar(64) NULL COMMENT "企业负责人",
    `address` varchar(512) NULL COMMENT "住所",
    `ope_address` varchar(512) NULL COMMENT "经营场所",
    `ope_type` varchar(64) NULL COMMENT "经营方式",
    `ope_scope_2002` text NULL COMMENT "经营范围(2002)",
    `ope_scope_2017` text NULL COMMENT "经营范围(2017)",
    `warehouse_address` varchar(512) NULL COMMENT "库房地址",
    `issue_date` date NULL COMMENT "发证日期/备案日期",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-医疗器械经营许可"'''

    dwd_business_info_import_medical_device = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `kind` varchar(64) NULL COMMENT "资质类别",
    `data_type` varchar(64) NULL COMMENT "数据类型",
    `address` varchar(512) NULL COMMENT "住所",
    `production_address` varchar(512) NULL COMMENT "生产地址",
    `reg_firm_name` varchar(255) NULL COMMENT "注册企业名称",
    `reg_firm_eid` varchar(64) NULL COMMENT "注册企业eid",
    `agency_address` varchar(255) NULL COMMENT "代理人住所",
    `product_en_name` varchar(255) NULL COMMENT "产品名称(英文)",
    `product_desc` varchar(1000) NULL COMMENT "产品描述",
    `manage_type` varchar(64) NULL COMMENT "管理类别",
    `product_model` text NULL COMMENT "规格型号",
    `structure` text NULL COMMENT "结构及组成",
    `apply_socpe` text NULL COMMENT "适用范围",
    `country_en` varchar(64) NULL COMMENT "生产国或地区(英文)",
    `attach` varchar(255) NULL COMMENT "附件",
    `other_content` varchar(255) NULL COMMENT "其他内容",
    `attention` varchar(512) NULL COMMENT "注",
    `remark` varchar(1000) NULL COMMENT "备注",
    `producer_name` varchar(64) NULL COMMENT "生产厂商名称（中文）",
    `product_name` varchar(255) NULL COMMENT "产品名称（中文）",
    `standard` varchar(64) NULL COMMENT "产品标准",
    `country_cn` varchar(64) NULL COMMENT "生产国或地区（中文）",
    `service_name` varchar(64) NULL COMMENT "售后服务机构",
    `service_eid` varchar(64) NULL COMMENT "售后服务机构eid",
    `change_date` date NULL COMMENT "变更日期",
    `components` varchar(64) NULL COMMENT "主要组成成分（体外诊断试剂）",
    `intended_use` text NULL COMMENT "预期用途（体外诊断试剂）",
    `storage` varchar(255) NULL COMMENT "产品储存条件及有效期（体外诊断试剂）",
    `department` varchar(64) NULL COMMENT "审批部门",
    `change_records` text NULL COMMENT "变更情况",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-进口医疗器械"'''

    dwd_business_info_medical_device_produce_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(1000) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `kind` varchar(64) NULL COMMENT "资质类别",
    `legal_rep` varchar(64) NULL COMMENT "企业法定代表人",
    `principal` varchar(64) NULL COMMENT "企业负责人",
    `address` varchar(255) NULL COMMENT "住所",
    `production_address` varchar(512) NULL COMMENT "生产地址",
    `production_scope` text NULL COMMENT "生产范围",
    `issue_date` date NULL COMMENT "发证日期/备案日期",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-医疗器械生产企业"'''

    dwd_business_info_domestic_medical_reg = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `kind` varchar(64) NULL COMMENT "资质类别",
    `data_type` varchar(64) NULL COMMENT "数据类型",
    `address` varchar(512) NULL COMMENT "住所",
    `production_address` varchar(512) NULL COMMENT "生产地址",
    `agency_name` varchar(64) NULL COMMENT "代理人名称",
    `agency_eid` varchar(64) NULL COMMENT "代理人eid",
    `agency_address` varchar(64) NULL COMMENT "代理人住所",
    `product_name` varchar(255) NULL COMMENT "产品名称",
    `product_model` text NULL COMMENT "规格型号",
    `structure` text NULL COMMENT "结构及组成",
    `product_desc` text NULL COMMENT "产品描述",
    `manage_type` varchar(64) NULL COMMENT "管理类别",
    `apply_socpe` text NULL COMMENT "适用范围",
    `other_content` varchar(255) NULL COMMENT "其他内容",
    `remark` text NULL COMMENT "备注",
    `attention` varchar(512) NULL COMMENT "注",
    `appr_date` date NULL COMMENT "批准日期",
    `attach` varchar(255) NULL COMMENT "附件",
    `standard` varchar(255) NULL COMMENT "产品标准",
    `change_date` date NULL COMMENT "变更日期",
    `zip_code` varchar(64) NULL COMMENT "邮编",
    `components` varchar(512) NULL COMMENT "主要组成成分（体外诊断试剂）",
    `intended_use` text NULL COMMENT "预期用途（体外诊断试剂）",
    `storage` varchar(255) NULL COMMENT "产品储存条件及有效期（体外诊断试剂）",
    `change_records` text NULL COMMENT "变更情况",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-国产医疗器械注册"'''

    dwd_business_info_mine_survey_right = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `kind` varchar(64) NULL COMMENT "资质类别",
    `project_name` varchar(255) NULL COMMENT "项目名称",
    `project_type` varchar(64) NULL COMMENT "项目类型",
    `survey_unit` varchar(64) NULL COMMENT "勘察单位",
    `survey_mineral` varchar(64) NULL COMMENT "勘察矿种",
    `coordinate` varchar(64) NULL COMMENT "极值坐标",
    `area` varchar(64) NULL COMMENT "面积",
    `location` varchar(255) NULL COMMENT "地理位置",
    `pub_date` date NULL COMMENT "公告日期",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-探矿权"'''

    dwd_business_info_mine_exploit_right = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(255) NULL COMMENT "数据源链接",
    `kind` varchar(64) NULL COMMENT "资质类别",
    `project_type` varchar(64) NULL COMMENT "状态/项目类型",
    `mine_name` varchar(255) NULL COMMENT "矿山/油气田名称",
    `mine_kind` varchar(64) NULL COMMENT "主矿种",
    `exploit_mode` varchar(64) NULL COMMENT "开采方式",
    `scale` varchar(64) NULL COMMENT "涉及生产规模",
    `area` varchar(64) NULL COMMENT "面积",
    `coordinate` varchar(255) NULL COMMENT "极值坐标",
    `location` varchar(255) NULL COMMENT "地理位置",
    `pub_date` date NULL COMMENT "公告日期",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-采矿权"'''

    dwd_business_info_network_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(512) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(64) NULL COMMENT "数据源链接",
    `equipment_name` varchar(64) NULL COMMENT "设备名称",
    `equipment_model` varchar(64) NULL COMMENT "设备型号",
    `producer_name` varchar(64) NULL COMMENT "生产企业",
    `producer_eid` varchar(64) NULL COMMENT "生产企业eid",
    `kind` varchar(64) NULL COMMENT "证书类别",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-电信设备进网许可"'''

    dwd_business_info_safe_product_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(512) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `license_form` varchar(64) NULL COMMENT "证书形式",
    `legal_rep` varchar(64) NULL COMMENT "法人代表",
    `reg_address` varchar(255) NULL COMMENT "注册地址",
    `economic_type` varchar(64) NULL COMMENT "经济类型",
    `issue_date` date NULL COMMENT "核发日期",
    `last_validity_date` date NULL COMMENT "前一次有效期",
    `extend_date` date NULL COMMENT "延期有效期",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-安全生产许可证"'''

    dwd_business_info_radio_customs_approve = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(255) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(64) NULL COMMENT "数据源链接",
    `country` varchar(64) NULL COMMENT "发货国家",
    `customs` varchar(64) NULL COMMENT "到货海关",
    `manufacturer` varchar(64) NULL COMMENT "生产厂商",
    `product_name` varchar(64) NULL COMMENT "商品名称",
    `product_model` varchar(64) NULL COMMENT "型号",
    `frequency_range` varchar(255) NULL COMMENT "频率范围",
    `transmit_power` varchar(64) NULL COMMENT "发射功率",
    `unit` varchar(64) NULL COMMENT "单位",
    `number` varchar(64) NULL COMMENT "数量",
    `model_check_num` varchar(64) NULL COMMENT "型号核准代码",
    `issue_date` date NULL COMMENT "核发日期",
    `remark` varchar(64) NULL COMMENT "备注",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-无线电发射设备进关核准"'''

    dwd_business_info_radio_model_approve = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `cer_agency` varchar(64) NULL COMMENT "发证机关",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(64) NULL COMMENT "数据源链接",
    `product_name` varchar(255) NULL COMMENT "设备名称",
    `product_model` varchar(255) NULL COMMENT "设备型号",
    `remark` varchar(512) NULL COMMENT "备注",
    `frequency_tolerance` varchar(255) NULL COMMENT "频率容限",
    `frequency_range` varchar(512) NULL COMMENT "频率范围",
    `transmit_power` varchar(512) NULL COMMENT "发射功率",
    `bandwidth` varchar(512) NULL COMMENT "占用带宽",
    `transmit_limit` varchar(255) NULL COMMENT "杂散发射限制",
    `appr_code` varchar(64) NULL COMMENT "核准代码",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-无线电发射设备型号核准"'''

    dwd_business_info_serv_authen = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` text NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(1000) NULL COMMENT "数据源链接",
    `location` varchar(64) NULL COMMENT "组织地址",
    `zip_code` varchar(64) NULL COMMENT "邮政编码",
    `info_report_date` date NULL COMMENT "信息上报日期",
    `mark` varchar(64) NULL COMMENT "证书使用的认可标识",
    `authen_n_basis` varchar(512) NULL COMMENT "认证依据",
    `annex_download_url` varchar(512) NULL COMMENT "证书附件下载",
    `cer_agency` varchar(64) NULL COMMENT "发证机构名称",
    `agency_eid` varchar(64) NULL COMMENT "发证机构eid",
    `agency_no` varchar(64) NULL COMMENT "发证机构批准号",
    `agency_date` varchar(64) NULL COMMENT "发证机构有效期",
    `agency_phone` varchar(64) NULL COMMENT "发证机构电话",
    `agency_domain` varchar(64) NULL COMMENT "发证机构网址",
    `agency_status` varchar(64) NULL COMMENT "发证机构状态",
    `agency_address` varchar(255) NULL COMMENT "发证机构地址",
    `agency_scope` text NULL COMMENT "发证结构业务范围",
    `agency_punishment_info` varchar(64) NULL COMMENT "发证机构处罚信息",
    `pause_start_date` date NULL COMMENT "暂停开始日期",
    `pause_end_date` date NULL COMMENT "暂停结束日期",
    `change_date` date NULL COMMENT "变更日期",
    `rev_date` date NULL COMMENT "撤销日期",
    `first_date` date NULL COMMENT "初次获证日期",
    `sup_times` varchar(64) NULL COMMENT "监督次数",
    `re_authen_times` varchar(64) NULL COMMENT "再认证次数",
    `authen_item` varchar(64) NULL COMMENT "认证项目",
    `cover_business_scope` text NULL COMMENT "认证覆盖的业务范围",
    `is_cover_multi_address` varchar(64) NULL COMMENT "是否覆盖多场所",
    `cover_address` varchar(512) NULL COMMENT "认证覆盖的场所名称及地址",
    `change_type` varchar(64) NULL COMMENT "变更类别",
    `is_sub_cert` varchar(64) NULL COMMENT "是否是子证书",
    `main_cert_no` varchar(64) NULL COMMENT "主认证证书号",
    `producer_name` varchar(255) NULL COMMENT "获证组织/生产企业名称",
    `producer_eid` varchar(64) NULL COMMENT "获证组织/生产企业eid",
    `producer_area` varchar(64) NULL COMMENT "获证组织所在国别和地区",
    `producer_area_code` varchar(64) NULL COMMENT "获证组织所在国别和地区编码",
    `cover_staff_num` varchar(64) NULL COMMENT "本证书体系覆盖人数",
    `producer_code` varchar(64) NULL COMMENT "获证组织统一社会信用代码/组织机构代码",
    `producer_address` varchar(512) NULL COMMENT "获证组织地址",
    `authen_staff_num` varchar(64) NULL COMMENT "认证相关员工数量",
    `authen_scope` varchar(64) NULL COMMENT "认证范围",
    `production_info` varchar(64) NULL COMMENT "产品信息",
    `change_history` text NULL COMMENT "证书变化历史轨迹",
    `authen_type` varchar(64) NULL COMMENT "认证类型",
    `authen_level` varchar(64) NULL COMMENT "认证级别",
    `authen_model` varchar(64) NULL COMMENT "认证模式",
    `rev_reason` varchar(64) NULL COMMENT "撤销原因",
    `cancel_date` date NULL COMMENT "注销日期",
    `change_cert_date` date NULL COMMENT "换证日期",
    `src_agency_no` varchar(64) NULL COMMENT "原颁证机构批准号",
    `src_license_no` varchar(64) NULL COMMENT "原认证证书号",
    `gap_no` varchar(64) NULL COMMENT "GAP注册号",
    `authen_field` varchar(64) NULL COMMENT "服务认证所属领域",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-服务认证"'''

    dwd_business_info_telecom_license = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `license_name` varchar(64) NULL COMMENT "证书名称",
    `license_no` varchar(64) NULL COMMENT "证书编号",
    `license_type` varchar(64) NULL COMMENT "证书类型",
    `license_content` varchar(64) NULL COMMENT "资质内容",
    `license_scope` varchar(64) NULL COMMENT "资质范围",
    `validity_date_start` date NULL COMMENT "有效期始",
    `validity_date_end` date NULL COMMENT "有效期止",
    `updated_time` date NULL COMMENT "更新时间",
    `approval_date` date NULL COMMENT "批准日期",
    `affirm_dep` varchar(64) NULL COMMENT "认定部门名称",
    `status_code` varchar(64) NULL COMMENT "状态代码",
    `province_code` varchar(64) NULL COMMENT "省份代码",
    `related_firm` varchar(64) NULL COMMENT "关联企业",
    `source` varchar(64) NULL COMMENT "数据来源",
    `url` varchar(512) NULL COMMENT "数据源链接",
    `legal_rep` varchar(64) NULL COMMENT "法定代表人",
    `business_kind` varchar(255) NULL COMMENT "业务种类",
    `cover_scope` text NULL COMMENT "覆盖范围",
    `reg_address` varchar(255) NULL COMMENT "注册地址",
    `reg_cap` decimal(38, 6) NULL COMMENT "注册资本（万元）",
    `licence_kind` varchar(512) NULL COMMENT "许可证业务种类",
    `ent_nature` int NULL COMMENT "企业性质",
    `is_quoted` varchar(64) NULL COMMENT "上市情况",
    `stock_no` varchar(64) NULL COMMENT "股票代码",
    `complaint_phone_num` varchar(64) NULL COMMENT "客户服务投诉电话",
    `complaint_cnt` int NULL COMMENT "客户投诉量",
    `complaint_reply_rate` varchar(64) NULL COMMENT "客户投诉回复率",
    `report_firm_name` varchar(64) NULL COMMENT "年报公示企业名称",
    `report_firm_eid` varchar(64) NULL COMMENT "年报公示企业eid",
    `create_time` bigint NULL COMMENT "创建时间",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "数据库更新时间",
    `is_history` int NULL COMMENT "是否历史数据",
    `is_delete` int NULL COMMENT "是否删除",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "企业资质-电信许可证书"'''

    dwd_essential_info_telephone = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `phone_lable` varchar(64) NULL COMMENT "电话号码标签（来源说明）",
    `phone` varchar(64) NULL COMMENT "电话号码",
    `get_date` date NULL COMMENT "获取日期",
    `source` varchar(64) NULL COMMENT "数据来源",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "本地数据库更新时间",
    `url` text NULL COMMENT "数据来源网站",
    `syx_source` int NULL COMMENT "数据来源（内部数据源）",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "电话号码表"'''

    dwd_essential_info_firm_address = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(64) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(1024) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(255) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `address` varchar(1000) NULL COMMENT "地址",
    `address_type` varchar(64) NULL COMMENT "地址类别",
    `address_code` varchar(64) NULL COMMENT "地址区域代码",
    `post_code` varchar(64) NULL COMMENT "邮编",
    `get_date` date NULL COMMENT "获取日期",
    `check_date` date NULL COMMENT "核准日期",
    `update_date` date NULL COMMENT "更新日期",
    `source` varchar(64) NULL COMMENT "数据来源",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "本地数据库更新时间",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "基本信息-企业地址"'''

    dwd_essential_info_firm_emails = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(255) NULL COMMENT "唯一id",
    `md5_id` varchar(64) NOT  NULL COMMENT "唯一id(md5_id)",
    `firm_eid` varchar(64) NULL COMMENT "企业eid",
    `firm_id` varchar(64) NULL COMMENT "企业id",
    `firm_name` varchar(64) NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `emails_lable` varchar(64) NULL COMMENT "邮箱标签（来源说明）",
    `emails` varchar(255) NULL COMMENT "邮箱",
    `get_date` date NULL COMMENT "获取日期",
    `source` varchar(64) NULL COMMENT "数据来源",
    `is_history` int NULL COMMENT "是否历史",
    `is_delete` int NULL COMMENT "是否删除",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) COMMENT "本地数据库更新时间",
    PRIMARY KEY (`md5_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC comment = "基本信息-企业邮箱"'''

    dwd_operational_risk_environment_punishment_info = '''CREATE TABLE if not exists %s.%s (
    `id` varchar(512) NOT NULL COMMENT "唯一id",
    `firm_eid` varchar(64) NOT NULL COMMENT "企业eid",
    `firm_id` varchar(64) NOT NULL COMMENT "企业id",
    `firm_name` varchar(1024) NOT NULL COMMENT "企业名称",
    `usc_code` varchar(64) NOT NULL COMMENT "统一社会信用代码",
    `reg_no` varchar(64) NOT NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `penal_id` varchar(64) NULL COMMENT "行政处罚记录编号",
    `illegal_type` varchar(64) NULL COMMENT "违法行为类型",
    `illegal_facts` text NULL COMMENT "违法事实",
    `illegal_comment` text NULL COMMENT "违法行为",
    `penal_reason` varchar(64) NULL COMMENT "处罚事由",
    `penal_basis` text NULL COMMENT "处罚依据",
    `penal_type` varchar(64) NULL COMMENT "处罚种类",
    `penal_type_cn` varchar(64) NULL COMMENT "处罚种类中文",
    `penal_comment` varchar(64) NULL COMMENT "处罚内容",
    `admin_penal_agency` varchar(255) NULL COMMENT "处罚机关",
    `penal_validity` varchar(64) NULL COMMENT "处罚有效期",
    `deci_docu_no` varchar(512) NULL COMMENT "处罚决定文书号",
    `deci_docu_url` varchar(1000) NULL COMMENT "处罚决定文书url",
    `deci_agency` varchar(64) NULL COMMENT "决定机关",
    `penal_deci_date` date NULL COMMENT "处罚日期",
    `penal_amt` double NULL COMMENT "处罚金额",
    `penal_deci_book_date` date NULL COMMENT "处罚决定书签发日期",
    `publicity_date` date NULL COMMENT "公示日期",
    `publicity_deadline` date NULL COMMENT "公示截止期",
    `publicity_done` varchar(255) NULL COMMENT "处罚执行情况",
    `admin_rew_result` varchar(64) NULL COMMENT "行政复议结果",
    `punishment_result` text NULL COMMENT "处罚结果",
    `end_date` varchar(64) NULL COMMENT "处罚截止期",
    `now_status` varchar(64) NULL COMMENT "当前状态",
    `province` varchar(64) NULL COMMENT "省份",
    `punishment_measure` varchar(64) NULL COMMENT "处罚措施",
    `punishment_title` varchar(512) NULL COMMENT "处罚标题",
    `is_history` int NULL COMMENT "是否历史",
    `unq_id` varchar(255) NOT NULL COMMENT "CDC唯一id",
    `local_row_update_time` datetime(6) NULL DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时\r\n间",
    `md5_id` varchar(64) NOT NULL COMMENT "加密唯一id",
    `is_delete` int NULL COMMENT "记录是否已删除",
    PRIMARY KEY (`md5_id`)
) comment = "经营风险-环保处罚"'''

    dwd_essential_info_reg_no_index = '''CREATE TABLE if not exists %s.%s (
    `firm_eid` varchar(64) NOT NULL COMMENT "企业eid",
    `firm_name` varchar(1024)  NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(255) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `ope_status` bigint NULL COMMENT "经营状态",
    `legal_rep` varchar(512) NULL COMMENT "法定代表人",
    `est_date` date NULL COMMENT "成立日期",
    `appr_date` date NULL COMMENT "核准日期",
    `md5_id` varchar(64) NOT NULL COMMENT "对应元素MD5值",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    PRIMARY KEY (`firm_eid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC  comment = "基本信息-注册号索引表"'''

    dwd_essential_info_usc_code_index = '''CREATE TABLE if not exists %s.%s (
    `firm_eid` varchar(64) NOT NULL COMMENT "企业eid",
    `firm_name` varchar(1024)  NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(255) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `ope_status` bigint NULL COMMENT "经营状态",
    `legal_rep` varchar(512) NULL COMMENT "法定代表人",
    `est_date` date NULL COMMENT "成立日期",
    `appr_date` date NULL COMMENT "核准日期",
    `md5_id` varchar(64) NOT NULL COMMENT "对应元素MD5值",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    PRIMARY KEY (`firm_eid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC  comment = "基本信息-统一社会信用代码索引表"'''

    dwd_essential_info_firm_name_index = '''CREATE TABLE if not exists %s.%s (
    `firm_eid` varchar(64) NOT NULL COMMENT "企业eid",
    `firm_name` varchar(1024)  NULL COMMENT "企业名称",
    `usc_code` varchar(64) NULL COMMENT "统一社会信用码",
    `reg_no` varchar(255) NULL COMMENT "注册号",
    `org_code` varchar(64) NULL COMMENT "组织机构代码",
    `ope_status` bigint NULL COMMENT "经营状态",
    `legal_rep` varchar(512) NULL COMMENT "法定代表人",
    `est_date` date NULL COMMENT "成立日期",
    `appr_date` date NULL COMMENT "核准日期",
    `md5_id` varchar(64) NOT NULL COMMENT "对应元素MD5值",
    `is_delete` int NULL COMMENT "记录是否已删除",
    `local_row_update_time` datetime(6) DEFAULT CURRENT_TIMESTAMP(6) ON UPDATE CURRENT_TIMESTAMP(6) COMMENT "数据库行更新时间",
    PRIMARY KEY (`firm_eid`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 ROW_FORMAT=DYNAMIC  comment = "基本信息-名称索引表"'''
